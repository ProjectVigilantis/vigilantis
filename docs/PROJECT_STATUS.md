# Vigilantis 프로젝트 현황 (PROJECT STATUS — SSOT)

> **이 문서가 프로젝트 범위·확정 결정·역할의 단일 기준(Single Source of Truth)이다.**
> 다른 문서와 충돌하면 **이 문서가 이긴다**(신뢰 우선순위는 아래 §문서 지도).
> **이 문서만 읽고도 프로젝트를 이해하고 진행할 수 있어야 한다** — 대조해야 할 상위 확정본은 없다. 내용이 부족하면 다른 문서를 가리키지 말고 이 문서를 채운다.
> **갱신은 주 2회 — 매주 월요일 아침과 목요일 회의 뒤, PM(김세혁)이 한다**(2026-09-03 확정, 아래 §확정 결정 로그). 월요일은 주차 전환·판정 기준, 목요일은 그 주 회의의 확정 결정·현황 반영이다. 그 밖의 주중 갱신은 마일스톤에 혼동을 줄 변경·큰 변경에 한한 예외다(아래 §현재 위치).
>
> **최종 갱신**: 2026-09-16 (김세혁) — **9/17(목) 정기 갱신을 하루 앞당겼다.** 2026-09-16(수) 긴급 회의가 팀 구성을 5인에서 4인으로 바꾸고 담당 DOMAIN을 전부 재배치했기 때문이다(`CLAUDE.md` §갱신 트리거의 "팀원 역할·담당 경계 변경"). **9/17(목)에는 그날 회의에서 새로 확정된 결정·액션 아이템만 반영한다** — 이 갱신본에 이미 담긴 현황·담당 서술은 다시 쓰지 않는다(`CLAUDE.md` §갱신 주체와 시점의 "앞당긴 주" 규약). 주차 전환·판정 기준은 그대로 9/21(월) 몫이다.

---

## 한 줄 요약

24/7 AWS 자산·보안 상시 관제 + 4단계 AI 가드레일 기반 원클릭 자율 조치 + 양방향 회복(자동 원복/원클릭 해제)을 제공하는 FinSecOps 플랫폼. **팀은 2026-09-16(수)부터 4인이다**(유건희 이탈 — 아래 §팀 & 역할). **9/30(수)까지 E2E 시연물을 완성**해 **10/1(목) 중간 발표에서 시연**하며, 그 발표는 **Connect Day 10/1(목)–10/2(금) 1일차 세션**이다. **MVP 마감 판정은 10/15(목)**, **최종 발표 12/11(금)에 Post-MVP 범위까지 배포·시연**한다.

## 현재 위치 (2026-09-16 기준)

> **갱신 주기**: 이 문서는 **주 2회, PM(김세혁)이 갱신한다** — **월요일 아침**(주차 전환·완료 요약·담당별 다음 단계·주차 종료 판정 기준)과 **목요일 회의 뒤**(그 회의의 확정 결정·액션 아이템·담당별 현황 반영. 주차 전환과 판정 기준은 건드리지 않는다). 2026-09-03 팀 결정으로 월요일 1회에서 바꿨다. 그 밖의 날에는 마일스톤에 혼동을 줄 변경이나 범위·API 계약·역할·확정 결정이 걸린 큰 변경이 있을 때만 예외로 갱신하며, 그 밖의 상황에서는 수정하지 않는다. 일반 기능·버그 수정·리팩터링·테스트 PR은 이 문서를 갱신하지 않는다.
> **개별 머지 이력은 이 문서에 복제하지 않는다** — 상세 원천은 [dev 머지 PR 목록](https://github.com/ProjectVigilantis/vigilantis/pulls?q=is%3Apr+is%3Amerged+base%3Adev), 결정 배경은 아래 §확정 결정 로그와 `docs/adr/`다.

### 발표 일정 (확정 — 2026-09-08 PM · 2026-09-14 MVP 마감일 추가 · 2026-09-16 Connect Day·시연물 마감 추가)

**이 표의 날짜가 프로젝트의 고정점이다.** 다른 문서·이슈·PR이 다른 날짜를 말하면 이 표가 이긴다.

| 날짜 | 무엇 | 범위 |
| --- | --- | --- |
| **2026-09-04(금)** ✅ 완료 | **기획 발표** | 기획·문서 단계까지. 구현 시연 없음 |
| **2026-09-30(수)** | **E2E 시연물 완성 마감** | 10/1(목)에 시연할 경로가 **이날 전부 서 있어야 한다.** 발표 자리가 아니라 **내부 마감**이며 아래 10/15(목)과 다른 날짜다(2026-09-16 긴급 회의 확정) |
| **2026-10-01(목)–10/02(금)** | **Connect Day(2일) — 1일차가 중간 발표 · MVP 시연** | ACC 카페 공간에 섹션을 나눠 멘토를 배정하고 팀당 **3–4시간** 멘토링을 받는다(설명–피드백–**평가** · 하루 2세션). **중간 발표는 별도 자리가 아니라 1일차 세션 그 자체다**(2026-09-16 확정). 런북 10종·양방향 회복·4단계 가드레일을 시연하며 **시연은 LocalStack 기반이다**(2026-09-14 PM 결정 — 아래 §확정 결정 로그). 제출물로 **프로젝트 기획서** 1부가 함께 나간다(#335 · 담당 박지현). **2일차 10/2(금)이 9주차 첫날과 겹친다** |
| **2026-10-15(목)** | **MVP 마감 판정** | 실 AWS 스모크 완료 + P2 3종 실 AWS 첫 검증까지. 발표 자리가 아니라 **내부 완료 판정일**이다 |
| **2026-12-11(금)** | **최종 발표** | **Post-MVP 범위 배포 및 시연** |

**주차 구간은 평일만 센다**(2026-09-08 PM 규칙 — 마일스톤 지정에서 주말·공휴일 제외). 그래서 주차 종료일은 항상 금요일이거나, 연휴가 걸리면 그 직전 평일이다.

| 주차 | 실작업 구간 | 성격 |
| --- | --- | --- |
| 1–2주차 | 8/11(화)–8/21(금) | 설계·환경 확정 ✅ |
| 3주차 | 8/24(월)–8/28(금) | 실행 기반 착수 ✅ |
| 4주차 | 8/31(월)–9/04(금) | P0 전반부 ✅ · **9/4(금) 기획 발표** |
| 5주차 | 9/07(월)–9/11(금) | P0 완결 · 🚦 9/11(금) 내부 P0 게이트 ✅ |
| **6주차** | **9/14(월)–9/18(금)** · 실작업 **5일** | **E2E 통합 · 백엔드 연동 완성** ← 현재 |
| 7주차 | 9/21(월)–**9/23(수)** · 실작업 **3일** | P1 2종 · **LocalStack 전 시나리오 실경로화** — **추석 연휴 9/24(목)–9/25(금)**. **기획서(#335)는 6주차로 앞당겨 이 주에 없다** — 박지현 부재 9/20(일)–9/30(수) |
| 8주차 | 9/28(월)–**10/01(목)** · 실작업 **4일** | **릴리스 컷 9/28(월)**(김세혁) → 리허설 9/29(화)–9/30(수) — **대본 진행·판정 기록 김승철 · 운영 머신·환경 김세혁**(2026-09-16 확정 · 박지현 부재) → **9/30(수) E2E 시연물 완성 마감** → **10/1(목) 중간 발표 = Connect Day 1일차 세션** |
| **9주차** | **10/02(금)–10/08(목)** · 실작업 **4일**(스모크 실작업은 **3일**) | **10/2(금) Connect Day 2일차** · **10/5(월) 개천절 대체 휴일** · **10/6(화) 업무 재배정** · **실 AWS 스모크 10/6(화)–10/8(목)** — [ADR-0006](adr/0006-localstack-team-standard-env.md) §4 이월 목록 해소 |
| **10주차** | **10/12(월)–10/15(목)** · 실작업 **4일** | P2 3종 실 AWS 첫 검증 · **10/15(목) MVP 마감 판정** — 앞의 **한글날 10/9(금)** 은 휴일이라 구간 밖 |
| 11–18주차 | 10/16(금)–12/11(금) | **Post-MVP · 12/11(금) 최종 발표(배포·시연).** 범위는 MVP 마감 판정 뒤 확정 — 성탄절은 구간 밖 |

> 🔴 **2026-09-16(수) 긴급 회의 — 팀이 4인이 됐다(유건희 이탈).** 담당 DOMAIN을 전부 재배치했고 **범위와 일정은 줄이지 않았다.** `FE` 전부가 **김세혁**에게, QA(Golden·시나리오·pytest·게이트 판정서) 전부가 **김승철**에게 갔고, **박지현은 프로젝트 기획서 전담**이다(§팀 & 역할). 같은 회의에서 **9/30(수) E2E 시연물 완성 마감**, **Connect Day 10/1(목)–10/2(금)**, **10/6(화) 업무 재배정**이 확정됐다.
>
> ⚠️ **일정이 두 번 바뀌었다. 지금 기준은 이것이다**(2026-09-14 PM 결정 · 2026-09-16 회의로 마감 1건 추가).
> **중간 발표 10/1(목)은 고정점 그대로다** — 2026-09-08에 10/15에서 2주 당겨진 뒤 바뀌지 않았다.
> **바뀐 것은 그 뒤다.** 종전에는 10/1(목) 발표로 MVP가 끝났는데, **MVP 마감 판정일 10/15(목)** 을 따로 두고 **실 AWS 스모크를 7주차(9/21–9/23)에서 9주차(10/02–10/08)로 옮겼다.** P2 3종 실 AWS 첫 검증은 10주차(10/12–10/15)다.
> **10/1(목) 시연은 LocalStack 기반이며 이는 의도된 결정이다** — 미비가 아니다. 실 AWS 전환(`AWS_ENDPOINT_URL` 제거)은 9주차(10/02–10/08) 스모크에서 처음 수행한다.
> **시연 축과 개발 축을 브랜치로 가른다** — 시연은 `main`(9/28(월) 릴리스 컷 + 태그), 개발은 `dev`가 계속 흐른다. **8주차(9/28–10/01)에 `dev`를 얼리지 않으므로 담당별 작업 배정이 발표를 이유로 끊기지 않는다.**
> 그래도 만회 구간이 넉넉하지는 않다 — 7주차(9/21–9/23)는 추석으로 3일, 8주차(9/28–10/01)는 **9/30(수) 시연물 완성 마감까지 3일**이다. **6주차(9/14–9/18) 종료 시점에 시연 경로가 서 있어야 한다.** 미달 시 §일정 리스크 방침대로 **범위 축소가 아니라 인력 재배치**로 대응한다.
> **9/30(수)과 10/15(목)은 다른 마감이다** — 9/30(수)은 *10/1(목)에 시연할 것*이 서는 날이고, 10/15(목)은 *실 AWS 스모크와 P2 3종까지 포함한 MVP 완료 판정일*이다. **2026-09-16 회의의 "9월 30일까지 MVP"는 앞의 것을 가리키며, 10/15(목) 판정일은 그대로 유지된다.**

- **마일스톤**: **6주차(9/14(월)–9/18(금) · 실작업 5일) — E2E 통합 · 백엔드 연동 완성.** 목표 = **10/1(목) 시연 경로가 LocalStack에서 전부 실경로로 서는 것**이다. **주중에 팀이 4인이 됐고(2026-09-16 · 유건희 이탈) 담당을 재배치했으나 이번 주 목표와 판정 기준은 줄이지 않았다.** 6주차 절반까지 **판정 기준 6개 중 2개가 충족됐다** — ⓑ(Status Check 실패 주입 = **가능**)와 ⓔ(T2 전 구간 관통)가 PR #346의 LocalStack 실측으로 닫혔고, 그 선행이던 해제 후보 생성(#329)도 PR #337로 9/14(월)에 들어왔다. **남은 넷은 성격이 갈린다.** ① **ⓒ는 2/3까지 왔다** — #291 / PR #292(종료 처리)와 #183 / PR #293(승인 모달 사실값)이 9/15(화) 머지됐고, 남은 것은 **메인 대시보드 하나**(#294 / PR #351 · `CHANGES_REQUESTED`)다. 이 PR이 **FE mock 계층을 통째로 걷어냈으므로**(1,762줄) 이제 화면은 실 `core-api`에만 붙는다 ② **ⓐ·ⓓ는 담당이 바뀐 자리다** — 박지현 → 김승철 이관이며, ⓐ는 선행이 전부 닫혀 **테스트 본문만** 남았고 ⓓ는 **전사 기입안 PR #355가 열렸으나 날짜·판정 대조가 남아 종합 판정은 아직이다** ③ **ⓕ는 10/6(화) 스모크의 전제**이며 ADR-0009와 구성 스크립트가 PR #348에 있다. 근거 조회·시계열 차트·시간 슬라이드 차트는 여전히 범위 밖이다(§MVP 확정 범위). **미달 시 범위 축소가 아니라 인력 재배치·범위 외 작업 중단으로 대응한다**(§일정 리스크) — 2026-09-16 회의가 실제로 그 방침을 집행했다.
- **완료 (구간 요약)**
  - **1–2주차(8/11–8/21) · 설계와 환경 확정** — 모노레포 단일 백엔드 재편([ADR-0001](adr/0001-mvp-monorepo-structure.md)), Action Whitelist 10종 확정·코드화(AI 추천 분리), FE↔BE API 계약 DTO 확정, LangGraph 그래프 구조 확정([ADR-0005](adr/0005-langgraph-stateless-domain-graphs.md)), LocalStack 팀 표준 환경([ADR-0006](adr/0006-localstack-team-standard-env.md)), FE Next.js 16 스캐폴딩·mock 계층, 자산 수집·Rule Engine 1차와 CI 가동.
  - **2주차(8/18–8/21) · 실행 기반 구축** — DB 저장 계층(ORM 13종·Alembic baseline·Repository), Core API 앱 골격·조회 API 3종, WebSocket 실시간 상태 전송(`/api/v1/ws`), Golden Dataset 20건(`Verdict` 4종·`SkipReasonCode` 5종 전량 커버), CI LocalStack service container.
  - **3주차(8/24–8/28) · 실행 기반 착수** — **종료 판정 기준 6개 전부 충족(2026-08-26, 기한 8/28 대비 2일 조기).** executor ↔ 가드레일 ④ Dry-Run 호출 규약 확정([ADR-0007](adr/0007-guardrail-dryrun-executor-precheck-contract.md))과 `precheck()` 확정 10종 구현, 스펙 JSON 백업 모듈, 가드레일 ①② 구현과 거절 사유 코드 4단계 공용 Enum 통합, `AIModelClient` 경계·전송 페이로드 마스킹, `POST /actions/execute` 라우터·Idempotency 멱등 처리, `_is_prod` 태그 키·값 집합 확정(미해결 4번 종결), 자산 4종 수집·`RelationType` 6종 완성, CI PostgreSQL service container(미해결 5번 종결)와 `apps/web` lint·build 잡, 런북별 typed 파라미터 계약, FE 자산·인시던트·실행 화면 mock 연동. **미완 1건**: 실행 계열 테스트 하네스(#136) — 4주차로 이월.
  - **4주차(8/31–9/04) · P0 전반부** — **종료 판정 기준 6개 전부 충족**(아래 판정 표 각주). 자산 자동 원복 3축 완성(`RIGHTSIZING` 실행 #211 / `get_waiter` 2/2 Status Check 판정·`AWAITING_CLOSURE` 신설 #240 / `REVERT_SIZE` 실행·`AUTO_ON_FAILURE` 자동 발동 #241), 가드레일 ④ Dry-Run 연결과 4단계 종합 판정 조립(#208)·단계 순서 회귀(#224)·통합 회귀의 4단계 진입점 교체(#229), LangGraph FinOps 그래프(#209)와 AI 응답 품질 계측(#237 — 모델 호출 1,824회 실측)·기본 모델 확정(#252)·요약 프롬프트 v1과 재통과 절차(#243), Detection → Incident → AI 입력 계약과 두 모듈 자리(#254), 인시던트 종료 처리 API(#199), Risk Evaluator 확정·구현(#210), 수집 실패 대응(#221)·CloudWatch 재조회 제거와 스캔 주기 config 이관(#255)·`collection_status` 산출 교정과 리전 스코프(#231 · #261)·EBS 전이 상태 판정 `SKIP_UNSUPPORTED_STATE` 신설(#276)·위협 정형화 프로덕션화(#268), **Golden Dataset 30건**(FinOps 자산 18 + SecOps 이벤트 12) 정답 100% 완결과 자산 화면 골든 적재 경로(#262), 실행 계열 시드 헬퍼 conftest 통합(#233), `dev` → `main` 병합 이력 복구(#194). **미완**: v1.6 화면 개편 잔여(#291 종료 처리 배선 · #294 메인 대시보드) — 5주차(9/07–9/11)로 이월. <br>※ 이 줄의 골든 건수는 **4주차 종료 시점(dev `bf8c2d3`, 2026-09-04)의 실측값**이다. 종전 기재 `28건(자산 16 + 이벤트 12)`은 그 시점 기준으로도 틀렸다 — EBS 골든 2건(#264 / PR #280)이 2026-09-03에 머지돼 4주차 안에 들어왔는데 세지 않았다(#310 · 2026-09-11 재측정). **현재값으로 덮지 않는다** — 이 자리는 4주차의 기록이다. 현재값은 §MVP 확정 범위의 「Golden Dataset 현재값」에 있다.
  - **5주차(9/07–9/11) · P0 완결과 SecOps 축 연결** — **P0 런북 4종이 전부 dev에 들어왔다.** 보안 축 2종을 5주차에 채웠다(`NACL_ADD_DENY` #297 / PR #313 — 되돌릴 근거를 먼저 남기고 차단 · `NACL_RESTORE` #298 / PR #328 — 우리가 넣은 규칙만 확인하고 삭제. 선행이던 대상 기준 백업 조회 `latest_for_target` DB 배선 포함). **판정 → Incident 배선이 이어졌다**(#306 / PR #320, Intake에 METRIC 근거 PR #318) — 4주차부터 걸려 있던 "본문은 섰는데 부르는 자리가 없다"가 닫혔다. **SecOps 축이 접수까지 왔다**(위협 접수 Workflow #322 / PR #325). 스캔 파이프라인이 앱 기동으로 실제 돌고 중복 실행이 막힌다(#277 / PR #287 · PR #281 advisory lock), Agent Dispatcher가 인시던트를 AI 호출로 넘겨 승인 대기까지 올린다(#285 / PR #290). 골든은 **토폴로지 자산 9건 편입으로 자산 유형 7/7종·관계 6/6종이 처음 전부 섰고**(#271 / PR #314 · PR #308), EBS 전이 상태 골든 5건(#276 / PR #300), AI 산출 정답지 6건(#234 / PR #303)이 더해졌다. 게이트 준비로 골든 A1을 시드 실물에 바인딩해 T1-6을 실경로로 세웠다(#301 / PR #321 · 시드 NACL 대상 PR #319). ADR-0008 `Accepted` 전환(PR #296)으로 결정 로그 등재가 열렸고, 발표 3건 날짜가 확정 반영됐다(PR #312). **미완 4건** — ① `tests/test_e2e_scenario.py` 전 구간 흐름 skip 2건 미해제 ② FE 3건 전부 `CHANGES_REQUESTED`(#292 · #293 · #299) ③ **9/11(금) 게이트 판정서의 판정 칸 미기재**(R1–R11 전부 `☐`) ④ 차단 뒤 해제 후보 생성(#329) PR 전. 전부 6주차(9/14–9/18)로 이월한다.
- **다음 단계 (담당별 1줄 — 6주차(9/14–9/18) 카드 · 2026-09-16 4인 재편 반영)**
  - **김세혁** (`BE` · `INFRA` **+ `FE` 인수**): ✅ NACL 2종 실행 경로(#297 / PR #313 · #298 / PR #328) → ✅ **차단 성공 뒤 [해제] 후보 생성**(#329 / PR #337 — 9/14(월) 머지. 차단이 `SUCCESS`로 닫힌 그 주기에 백업에서 `NACL_RESTORE` 후보를 만들어 가드레일 4단계를 통과시킨다 — T2 [해제] 버튼의 근거) → ✅ **Status Check 실패 주입 = 가능 판정**(판정 기준 ⓑ · PR #346. 프로덕션 코드 무변경 — ADR-0006 §4 2행을 `impaired` 경로만으로 좁혔다(6차 개정). 시연이 지킬 조건 둘: ① 주입 창 = 실행 주기 뒤·판정 주기 전(`DISPATCH_INTERVAL_SECONDS` 기본 10초) ② 대기 조임 = `STATUS_CHECK_WAIT_*`(기본 3분)) → ✅ **T2 전 구간 관통 실측**(ⓔ · 같은 PR — 사람 조작 승인 2회) → ✅ Status Check 판정 불가 계약 `UNVERIFIED`(#249 / PR #341) → ✅ 다운사이징 목표 타입 서버 계산(#251 / PR #345) → ✅ AI 서명 차단 CI 잡(PR #340) → ✅ dev→main 시험 머지 통과(9/15(화)). **진행 중 — FE 인수분이 최우선**: 🔴 **DSH-001 메인 대시보드 + FE mock 계층 제거**(#294 / PR #351 · `CHANGES_REQUESTED`) — 유건희 PR #299가 닫히고 다시 올린 것이며, 여기서 mock Route Handler **6파일(1,762줄)** 이 통째로 빠졌다. **판정 기준 ⓒ에 남은 마지막 한 건이고, 10/1(목) 심사자가 보는 첫 화면이다.** 🔴 **실 AWS 스모크 전제**(ⓕ) — 계정·키·비용 통제 **ADR-0009**와 구성 스크립트가 PR #348(`CHANGES_REQUESTED`)에 있다. 10/6(화)에 바로 돌리려면 **10/1(목) 이전에 서 있어야 한다.** 🔴 **릴리스 컷 체크리스트(#349)** — 9/28(월) 컷 당일 절차와 컷 전 확인 목록. **그 밖**: 8주차(9/28–10/01) 릴리스 컷 — 시험 머지 통과(9/15(화)), 태그 이름은 컷 전에 정한다. FE mock 제거 후속 문서(#352 — **2026-09-16 재편으로 배정 보류 사유가 사라졌다**: 시연 문서 2곳·골든 README는 김승철, PR 템플릿·FE 실행 절차·대시보드 주석은 김세혁), dev Dependabot 경보 12건(#311). **조치별 절감 예상의 화면 표시**도 유건희 이탈로 이 카드로 왔다 — 추정값이 **필드 이름·화면 표기로 서버 파생 사실값과 구분**돼야 한다는 설계 조건을 그대로 승계한다.
  - **안성일** (`AI` · FastAPI 아키텍처 · DB): ✅ 가드레일 4단계 완성(#177 · #208) → ✅ LangGraph FinOps 그래프(#209 / PR #226) → ✅ AI 응답 품질 계측·모델 비교(#237 / PR #248, 기본 모델 교체 PR #252) → ✅ Detection → Incident → AI 입력 계약(#254 / PR #258) → ✅ AI 요약 프롬프트 v1·재통과 절차(#243 / PR #288 — `docs/AI_SUMMARY_BASELINE.md` 신설) → ✅ Agent Dispatcher(#285 / PR #290) → ✅ SecOps 위협 접수 Workflow(#322 / PR #325) → ✅ **LangGraph SecOps 그래프**(#323 / PR #331 — 6주차 첫날 9/14(월) 머지). `run_secops_graph`(`summarize_evidence → reassess_risk → propose_candidates → validate_output_contract`)와 dispatcher의 SECOPS 분기가 함께 섰다 — [ADR-0005](adr/0005-langgraph-stateless-domain-graphs.md)가 8/18에 확정해 둔 두 그래프 중 나머지 하나다. **진행 중**: 🔴 **조치별 절감 예상 구현(#347)** — 금액과 산출 근거를 생성·저장하고 조회로 제공한다. 2026-09-07 PM 결정으로 MVP에 편입된 축이며 **출력 계약 소유자라 주담당**이다. 화면 표시 접점은 이제 유건희가 아니라 **김세혁**이다. 🔴 **SecOps Incident 근거 고정 범위와 모의 입력 계약 보완(#350)** — **#324(SecOps AI 응답 품질 검증)의 선행이라 #324는 동결 중**이다. **확인 필요 해소**: 승인 화면 조치 대상 문맥의 서버 파생 전환(#183)은 유건희가 PR #293으로 닫았고(9/15(화) 머지) 이슈도 CLOSED다 — 담당이 갈려 있던 문제가 이탈과 함께 종결됐다.
  - **김승철** (`DATA` **+ QA 전부 인수**): ✅ Risk Evaluator(#210 / PR #206) → ✅ 수집 실패 대응(#221 / PR #222) → ✅ CloudWatch 수집 정밀화(#255 / PR #257) → ✅ EBS 전이·비정상 상태 판정 정책(#276 / PR #284) → ✅ `collection_status` 리전 스코프(#261 / PR #278) → ✅ 위협 정형화 프로덕션화(#268 / PR #269) → ✅ 스캔 스케줄러 lifespan 배선 + 중복 실행 방지(#277 / PR #287 · PR #281) → ✅ 판정 → Intake 배선(#306 / PR #320) → ✅ **수집이 소멸 자산을 표시하고 판정에서 뺀다**(#332 / PR #339 — 실물 없는 자산에 조치 버튼이 열리던 자리를 닫았다). **진행 중 — QA 인수분이 최우선**: 🔴 **T1·T2 전 구간 흐름 테스트 2건을 구현해 통과시키고 기존 skip을 해소한다**(판정 기준 ⓐ) — **T1·T2 모두 선행이 전부 닫혔고 남은 것은 테스트 본문이다.** 두 테스트는 상태 전이가 docstring 명세로만 있고 본문이 비어 있다. 재현 근거는 PR #346의 실측과 [`docs/E2E_DEMO_SCENARIOS.md`](E2E_DEMO_SCENARIOS.md) §대조 3번·§T2 관통 실측이다. **구현 위치는 `apps/core-api/tests/`로 둔다**(2026-09-16 확정) — 두 테스트가 Incident·Execution 상태 전이와 DB 백업 연결을 검증해야 하는데 `tests/conftest.py`는 **DB 비의존 계층**이라 `apps/core-api/tests/conftest.py`의 `db`·`pg_engine`·`client_pg`와 시드 픽스처를 상속하지 않는다. 그 경로는 CI `test` 잡의 pytest 목록에 **이미 있어** 워크플로 변경이 없다. 기존 `tests/test_e2e_scenario.py`에는 옮긴 위치 안내를 남긴다. 🔴 **9/11(금) 게이트 판정서의 판정 칸을 채운다**([`docs/E2E_GATE_0911.md`](E2E_GATE_0911.md) §6 · 판정 기준 ⓓ · 이슈 #301) — R1–R11과 종합 판정이 전부 비어 있다. **게이트 당일 값을 재현할 수 없으므로 인수가 아니라 받아 적는 형태이며, 넘겨주는 원천이 둘이다** — **박지현(대본·사전 준비·T2 축)** 과 **김세혁(T1-3–T1-6 관통 축)** 이 각각 근거와 실측을 넘기고 김승철이 대조·전사한다. 원천이 갈리는 이유는 [`docs/E2E_GATE_0911.md`](E2E_GATE_0911.md) §6-1이 작성 머신의 R4·R5와 R6 실행 단계를 `🔬미실측`으로 구분했고 그 축의 실측이 **PM 로컬에 있기 때문이다**(#316 · §2-⓪). **#316의 값은 9/10(목) 것이라 9/11(금) 당일 결과로 그대로 전사하지 않는다** — 날짜·대상·측정 구간을 갈라 적는다. 전사 기입안은 **PR #355**로 열렸고 R3 대상 ARN·R4 재현율·R6 도착 타입·R10 전체 벽시계가 확인 대기다 — **칸이 채워진 것도 종합 판정이 선 것도 아니다.** **그 밖 — `DATA`**: ARN 조립 원천 통합·조인 무결성 검사(#342 / PR #353 — 가드레일 ③이 문자열 완전일치로 대조한다), 조회 경로 인덱스(#343 / PR #344 · `CHANGES_REQUESTED`), 다운사이징 규칙의 비율·하한 값 소유(#251). **유사 서비스 조사·화면 제작**(2026-09-07 확정 시점의 담당은 유건희 + 김승철 협업이었고, 이탈로 **김승철 단독**이 됐다 — 현재 상태를 확인해 끝낼지 접을지 정한다).
  - **박지현** (`DOCS` · **프로젝트 기획서 전담**): ✅ SecOps 정답 12건(PR #223 · #242) → ✅ EBS 골든 2건(#264 / PR #280) → ✅ 골든 FinOps 자산 적재 경로(#262 / PR #263) → ✅ 실행 계열 시드 헬퍼 conftest 통합(#233 / PR #260) → ✅ 골든 토폴로지 자산 9건(#271 / PR #314 — 자산 유형 7/7종·관계 6/6종이 처음 전부 섰다) → ✅ AI 산출 정답지 6건(#234 / PR #303) → ✅ 게이트 대본 확정과 T2 Incident 실경로화(#301 / PR #302 · #315 · #321 · **#330은 9/14(월) 머지**) → ✅ 골든 추출 스키마 드리프트 등식 가드(#309 / PR #326). **진행 중 — 이것 하나다**: 🔴 **Connect Day 제출용 프로젝트 기획서(#335)** — 제출은 **10/1(목) Connect Day 1일차**이고 그 자리가 **평가**다. 형식은 **Word**다. **원천은 둘이고 우선순위가 있다** — **필수 2항목(개발 일정 WBS · 팀원 R&R)은 이 문서(SSOT)가 원천**이며 값이 다르면 SSOT를 따른다(#335 완료 판정 기준이 SSOT 일치를 요구한다). **그 밖의 서술·도표·양식**은 Confluence와 Slack `문서버전 관리` 채널의 재료를 가져다 쓴다. **완료 판정을 대조하는 원천은 저장소 안이다** — Confluence·Slack은 재료이지 판정 기준이 아니다. 본문은 `docs/PROJECT_PROPOSAL.md`로 저장소에 두고 제출용 변환본을 거기서 내며, 필수 2항목이 옮겨 오는 자리는 이 문서의 §현재 위치 주차 표와 §팀 & 역할이다 — **4인 재편 뒤의 표를 옮긴다** — 9/4(금) 제출본의 5인 R&R과 "10/15 MVP 시연" 일정표는 둘 다 낡았다. **일정을 앞당겼다(2026-09-16)** — 착수 **6주차(9/14–9/18)** · 본문 완성 **9/18(금)** · PM 검토 **9/18(금)** · 제출 **10/1(목)** Connect Day 1일차. **박지현이 9/20(일)–9/30(수) 자리를 비우기 때문이다** — 종전 일정(착수 7주차(9/21–9/23) · 검토 9/28(월))은 착수일과 검토일이 **둘 다 그 구간 안**이라 실행할 수 없고, 9/28(월)에 지적이 나와도 반영할 사람이 없다. 9/16(수)에 이미 착수했으며 실작업은 **9/16(수) 반나절 + 9/17(목) + 9/18(금) = 2.5일**이다. **이관 완료**: Golden Dataset·E2E 시연 시나리오·pytest·게이트 판정서는 2026-09-16부로 김승철이 받았다 — 판정 기준 ⓐ·ⓓ 포함이다.
- **주차 종료 판정 기준 (6주차 · 9/14(월)–9/18(금)) — 이 문서가 유일한 기재 위치다**

  주차 종료 판정 기준은 `docs/PROJECT_STATUS.md` 밖 어디에도 두지 않는다. 매주 **월요일** 갱신에서 PM이 AI의 추천을 받아 직접 갱신한다 — 목요일 갱신은 이 표를 건드리지 않는다.

  **6주차(9/14–9/18)의 기준은 전부 "10/1(목) 시연 경로가 LocalStack에서 실경로로 서는가"를 묻는다.** 만회 구간이 7주차(9/21–9/23) 3일과 8주차(9/28–10/01) 4일뿐이므로, 여기서 미달한 항목은 사실상 대체 컷으로 굳는다.

  | # | 기준 | 판정 |
  | --- | --- | --- |
  | ⓐ | `tests/test_e2e_scenario.py`의 **전 구간 흐름 skip 2건 해제** — T1(자동 원복) · T2(차단 → 원클릭 해제). 두 테스트는 본문이 비어 있어 **선행이 닫혀도 본문을 써야 열린다** | ⏳ |
  | ⓑ | **LocalStack Status Check 실패 주입 가능 여부 판정** — `stop_instances`로 `_NOT_BOOTING_STATES` 분기에 도달하는지 실측하고, **가능/불가 어느 쪽이든 근거와 함께 문서에 남긴다.** 이 결과로 T1 7·8·9번이 10/1(목) 시연에서 실경로인지 대체 컷인지 갈린다 | ✅ |
  | ⓒ | FE가 실 API로 **자산 → 인시던트 → 종료 처리**까지 간다 — `CHANGES_REQUESTED` 3건(#294 / PR #299 · #291 / PR #292 · #183 / PR #293) 머지 | 🔶 2/3 |
  | ⓓ | **9/11(금) 게이트 판정서 기재 완료** — [`docs/E2E_GATE_0911.md`](E2E_GATE_0911.md) §6의 R1–R11과 종합 판정이 채워져 dev에 머지된다 | ⏳ |
  | ⓔ | **T2 전 구간이 LocalStack에서 관통한다** — 위협 접수(#322) → SecOps 그래프(#323) → 차단(`NACL_ADD_DENY`) → **해제 후보 생성(#329)** → 해제(`NACL_RESTORE`)까지 사람 조작이 승인 2회뿐이다 | ✅ |
  | ⓕ | **9주차(10/02–10/08) 실 AWS 스모크의 전제가 확보된다** — 계정·키·비용 통제 방침이 서고, P2 선행인 ALB·다중 EC2 시연 인프라 구성이 착수된다. 10/2(금)에 바로 돌리려면 10/1(목) 이전에 서 있어야 한다 | ⏳ |

  > **6주차(9/14–9/18) 진행 중간 기록(2026-09-16 기준)**: ⓑ ✅ **가능** — 실행 주기 뒤 대상 인스턴스를 `stop_instances`로 멈추면 판정이 `FAILED`로 떨어지고 `AUTO_ON_FAILURE` 자동 원복까지 LocalStack에서 실경로로 흐른다(PR #346). 미측정 전제였던 `IncludeAllInstances=True`가 멈춘 인스턴스를 돌려주는가는 **참이었다** ⓔ ✅ 위협 접수 → 해제까지 사람 조작 승인 2회로 관통했다(같은 PR). **한계 한 줄**: 경로 관통은 타이머를 띄운 앱이 아니라 **타이머가 부르는 함수를 같은 순서로 직접 불러** 확인했다 — 앱 기동 상태의 관통은 게이트 대본으로 확인한다 ⓒ 🔶 **2/3** — #291 / PR #292(종료 처리)와 #183 / PR #293(승인 모달 사실값)은 9/15(화) 머지됐고, 남은 것은 대시보드다. **유건희 PR #299는 닫혔고 김세혁이 PR #351로 다시 올렸다**(mock 계층 제거 포함 · `CHANGES_REQUESTED`) ⓐ·ⓓ ⏳ — **담당이 박지현 → 김승철로 바뀌었다**(2026-09-16 4인 재편). ⓐ는 선행이 전부 닫혀 본문만 남았고(구현 위치는 `apps/core-api/tests/`), ⓓ는 **전사 기입안 PR #355가 열렸다 — 날짜·판정 대조가 남아 종합 판정 확정은 아니다** ⓕ ⏳ — ADR-0009(계정·키·비용 통제)와 구성 스크립트가 PR #348에 있다.

  > **5주차(9/07–9/11) 판정 결과 — 4/6 충족 · 1건 부분 충족**: ⓐ ✅ NACL 2종 실행 경로 머지(`ADD_DENY` #297 / PR #313 · `RESTORE` #298 / PR #328). 선행이던 `latest_for_target` DB 배선도 함께 들어갔다 ⓑ ❌ **미충족** — 전 구간 흐름 skip 2건 그대로다. T2의 선행(#323)은 6주차 첫날 9/14(월)에 닫혔으나 테스트 본문이 없다 ⓒ ✅ 판정 → Intake → AI 호출 → 승인 대기 배선 완료(#306 / PR #320 · #285 / PR #290). 다만 **9/11(금) 게이트 당일에는 대본이 `SCAN_ENABLED=false`로 스캔을 꺼서 T1-2가 수동 우회로 시연됐다** — 배선이 아니라 대본이 그 경로를 껐다([`docs/E2E_GATE_0911.md`](E2E_GATE_0911.md) §3-2) ⓓ ✅ 스캔 파이프라인 앱 lifespan 배선(#277 / PR #287)과 advisory lock 중복 실행 방지(PR #281) ⓔ ❌ **미충족** — FE 3건이 전부 `CHANGES_REQUESTED`이고 2026-09-07 이후 움직이지 않았다 ⓕ 🔶 **부분 충족** — 게이트는 9/11(금)에 실제로 돌았고 T2 후반까지 관통 실측했으나, **판정서의 R1–R11과 종합 판정이 비어 있어 결과가 저장소에 남지 않았다.** 6주차 ⓓ로 넘긴다.

  > **4주차(8/31–9/04) 판정 결과 — 6/6 충족**: ⓐ `RIGHTSIZING` LocalStack 실행과 타입 반영 확인(`apps/core-api/services/tests/test_execute_localstack.py`), 백업은 AWS 변경 호출 이전 commit(`workflows.py::store_instance_spec_backup`) ⓑ `StatusCheckVerdict` 3분기(`OK`·`FAILED`·`TIMED_OUT`, `services/aws/rollback.py`) ⓒ 골든 FinOps → `FinOpsGraphInput` 변환기 위에서 모델 호출 1,824회 실측·확정 600회 환각 0(#237 / PR #248) ⓓ 가드레일 4단계 진입점 회귀(#229)·단계 순서 회귀(#224) green, CI 통과 ⓔ Golden 30건 정답 100% — §미해결 6번 종결 ⓕ 수집 부분 실패 시 재수집 동작 확인(#221 / PR #222).
  >
  > **ⓕ의 판정 단위(2026-09-07 PM 확정)**: 기준 문장의 "실패 대상만"은 **리전 단위**로 읽는다. C4가 구현한 것은 자산 단위 실패 사유 기록 + `PARTIAL` 마감 + **일시성 실패 리전의 1회 재시도**이며 자산 단위 재수집은 없다. 수집 실패의 실제 형태가 리전·서비스 단위 API 실패이고 자산 단위 누락은 다음 스캔 주기가 덮으므로 이 단위로 충족을 인정한다 — 자산 단위 재수집이 필요하다는 판단이 서면 그때 별도 카드다(김승철 질의에 대한 답, #207 코멘트).
  >
  > **ⓒ의 대상 건수**: 기준 문장의 "FinOps 10건"은 **목표선**이고, 실제 AI 대상은 4주차 종료 시점 골든 FinOps 자산 중 `COST_CANDIDATE` **6건**이다 — 나머지는 `SKIP`·`THREAT`·`UNUSED`라 규칙 엔진이 AI로 넘기지 않는다(`services/rule_engine.py`). 산출은 확인됐고, 그 산출을 **정답지로 고정하는 회귀**는 #234 / PR #303으로 5주차(9/07–9/11)에 닫혔다.
  >
  > **3주차(8/24–8/28) 판정 결과**: ⓐ Dry-Run 인터페이스 문서 합의 ⓑ `_is_prod` 정책 종결 ⓒ CI DB 통합 28건 skip 0건 ⓓ `POST /actions/execute` 멱등 처리 ⓔ FE 자산 화면 mock 100% 렌더 ⓕ LLM 전송 페이로드 마스킹 — **6/6 충족(2026-08-26)**.

- **6주차(9/14–9/18) 리스크** (2026-09-16 4인 재편 반영)
  1. 🔴 **FE가 한 사람에게 몰렸다** — 유건희 이탈로 `FE` 전부가 김세혁에게 갔고, 김세혁은 `BE`·`INFRA`와 **릴리스 컷·실 AWS 스모크 전제까지** 들고 있다. 남은 FE 한 건(#294 / PR #351 — 대시보드 + mock 제거)이 **10/1(목) 심사자가 보는 첫 화면**이고 지금 `CHANGES_REQUESTED`다. 같은 사람의 열린 PR이 셋(#351 · #348 · 그 밖)이라 **이번 주 최대 단일 리스크는 FE 미완이 아니라 한 사람의 직렬 처리량**이다. 처분: 아침 분배에서 김세혁 밖으로 뺄 수 있는 것(#352 문서 후속 등)을 먼저 넘긴다.
  2. 🔴 **QA 인수인계가 판정 기준 두 칸 위에서 일어난다** — 판정 기준 ⓐ(흐름 테스트 본문)와 ⓓ(9/11 게이트 판정서)가 **박지현 → 김승철 이관 대상 그 자체**다. ⓓ는 특히 **시간이 갈수록 복구 비용이 오르는 유일한 항목**이다 — 게이트를 돌린 값이 저장소에 없고(R1–R11 전부 `☐`), 같은 값을 다시 얻으려면 `docker compose down -v`부터 스택을 다시 세워야 하는데 그 사이 dev가 움직이면 같은 값이 나오지 않는다. **인수받는 사람이 그 값을 재현할 수 없다** — 그래서 ⓓ는 이관이 아니라 **넘겨주는 쪽이 받아 적게 하는 형태**여야 하고, **원천이 둘이다**: 박지현(대본·사전 준비·T2 축)과 김세혁(T1-3–T1-6 관통 축 — §6-1이 `🔬미실측`으로 가른 칸의 실측이 PM 로컬에 있다 · #316). **박지현이 9/20(일)부터 자리를 비우므로 그쪽 인계는 9/18(금)까지 끝나야 한다.** 기입안 PR #355가 대조를 시작했다.
  3. **T1 실패 주입은 섰고, 남은 것은 시연 운영이다**(PR #346 — 판정 기준 ⓑ 가능). 주입은 실행 주기가 끝난 뒤·다음 판정 주기 전에 해야 하고, 그 창을 놓치면 판정이 `OK`로 끝나 T1이 `SUCCESS`로 닫힌다 — **자동 원복 컷이 사라진다.** 리허설(9/29–9/30) 전에 주입 절차를 대본에 넣고, 사람이 시점을 맞추지 않아도 되도록 주입 헬퍼(인스턴스가 목표 타입으로 running이 되는 순간 정지)를 둘지 정한다.
  4. **9주차 스모크 실작업이 3일로 줄었다** — 10/2(금)이 Connect Day 2일차, 10/5(월)이 대체 휴일, 10/6(화)이 업무 재배정이라 **실 AWS 스모크는 사실상 10/6(화)–10/8(목)** 이다. 계정·키·비용 통제와 ALB·다중 EC2 인프라는 리드 타임이 있으므로 **전제는 6–7주차(9/14–9/23)에 서야 한다**(PR #348 · 판정 기준 ⓕ). 8주차(9/28–10/01)로 밀면 릴리스 컷·리허설·발표와 같은 4일에 겹친다.
  5. ✅ **릴리스 컷 시험 머지 — 통과(9/15(화))** — 충돌 0이고, 합친 결과 트리가 그날 `dev` 트리와 같았다(#239 이후 `main`에 새 내용이 없다). CI 경로 pytest 2016 passed / 2 skipped(흐름 테스트 무조건 보류) · web lint·build·유닛 47건 · AI 서명 0건. 그래서 컷 머지는 "그날의 `dev` 끝을 `main`에 올리는 것"과 같고, CI가 그 조합을 처음 보는 일은 없다. **남는 위험은 머지가 아니라 그날 `dev`에 시연 경로가 서 있는가**이며, 그 목록과 컷 당일 절차는 #349가 갖는다. 커밋 격차(9/15 기준 117커밋 · 계속 늘어난다)는 기준으로 쓰지 않는다 — 컷 당일 확인 기준은 "합친 결과 트리 = dev 트리"다.
  6. **AI가 낸 절감액이 사실값처럼 읽힐 수 있다** — 산출 근거의 소유자는 정해졌고(2026-09-07 PM 결정) 구현 카드는 #347이다. 남는 위험은 둘이다. ① 현행 계약은 승인 근거에 **서버가 파생한 사실값만** 싣는데(#154 → #178 → #183) 그 옆에 LLM 추정치가 놓인다 — **필드 이름·화면 표기로 구분**하지 않으면 관제자가 되돌릴 수 없는 조치를 추정치를 믿고 승인한다. ② #237이 세운 합격선은 **실행으로 나가는 값**의 흔들림 0이고 금액은 그 밖이라, 재현성·환각을 재는 자리를 따로 두지 않으면 계측되지 않은 숫자가 화면에 선다. **⚠️ 화면 표시 담당이던 유건희가 이탈했다** — 표시 쪽 설계 조건(추정값과 사실값의 시각적 구분)을 지킬 사람이 김세혁으로 옮겨졌고, 계약 쪽(#347)은 안성일 그대로다.

## MVP 확정 범위

- **관제**: AWS 단일 계정 / 1–2개 리전. **EC2·SG 중심** + 런북 조치 대상 리소스(NACL, EBS, ASG·Launch Template, ALB Target Group).
- **위협**: OpenIP(0.0.0.0/0)·SSH 브루트포스 — Golden Dataset 기반 **모의(Mock) 주입** (실환경 GuardDuty 연동은 Post-MVP).
- **Golden Dataset 현재값**(2026-09-14 실측 · 이슈 #310): **하나의 숫자로 적을 수 없다 — 답이 셋이고 뜻이 다르다.** ① **입력 자산 32건** — 골든이 DB에 적재하는 자산(`datasets/golden/finops/input/`, 유형 7/7종) ② **판정 정답 28건** — 규칙 엔진 판정의 정답이 있는 자산. `scripts/load_golden_assets.py --verify`가 대조하는 수이며, 차이 4건은 **그래프 노드로만 쓰이고 판정 대상이 아닌** 토폴로지 전용 유형(NACL·Launch Template·ASG·ALB Target Group 각 1건)이다 ③ **SecOps 이벤트 16건** — 입력 16 / 정답 16. **⚠️ ②의 28과, 이 문서가 4주차까지 적어 온 `28건`은 숫자만 같고 뜻이 전혀 다르다**(후자는 자산+이벤트 합계였고 그나마 틀린 값이었다). §현재 위치의 4주차 기록에 있는 건수는 **그 시점의 값**이며 현재값으로 덮지 않는다.
- **AI**: OpenAI `gpt-5.6-luna`(`reasoning_effort=low`) + Pydantic v2 Structured Output + **LangGraph 오케스트레이션**. CoT 3줄 요약 + Runbook ID 추천. LangGraph는 프로젝트 정체성으로 MVP 구현 확정(2026-08-13) — 출력 계약(Pydantic 스키마)은 동일하게 유지.
- **조치별 절감 예상**(2026-09-07 확정, 아래 결정 로그): 자산 인스턴스 수정 추천을 실행할 때 **얼마를 아낄 수 있는지**를 관제자가 확인할 수 있게 한다. **금액과 그 산출 근거를 모두 AI가 내고**(추천 호출에 함께), 근거는 구조화 필드로 **저장해 두고 필요할 때 그 저장분을 쓴다** — 볼 때마다 다시 부르지 않는다. **비용 추이 그래프·누적 절감 캘큘레이터(집계·추이 축)는 범위 밖**이며, 이 결정은 개별 추천의 절감 예상 하나에 한한다.
- **4단계 가드레일(순서 고정)**: ① Schema Check ➔ ② Action Whitelist ➔ ③ ARN Match ➔ ④ AWS Dry-Run.
- **레콘(Reckon) — Incident 상태 축의 이름**(2026-09-03 확정, 아래 결정 로그): 가드레일(*실행해도 되는가*) 다음의 두 물음 — *실제로 무엇이 일어났는가* → *그 결과를 어떻게 수습·종료하는가* — 를 다루는 **Incident 상태와 그 상태를 관리하는 일련의 과정**을 부르는 고유명사. '가드레일'과 같은 층위로 쓴다.
- **양방향 회복**: 자산 = 스펙 JSON 백업 ➔ `get_waiter` Status Check(2/2) ➔ 자동 원복 / 보안 = 선제 차단 ➔ 관제자 [원클릭 해제].
- **3단계 위험 대응**: High `PRE_MITIGATION_0_5S`(0.5초 선차단 시뮬레이션) / Medium·Low `AGENT_WAIT`(승인 대기) / **Medium 1분 미응답 `TIMEOUT_ISOLATION_1M`(자동 격리) — Low는 제외**(2026-08-25 확정, 아래 결정 로그). **화면 표기**: Medium 대기 화면에 초 단위 카운트다운을 두지 않고 `제안 생성 시간`·`실행 예정 시간` **절대 시각 2종**만 보여준다(2026-08-27 확정, 아래 결정 로그).
- **FE**: Next.js 16 + Shadcn UI, 위협 토폴로지 맵(붉은색 노드), One-Click + Idempotency Key. **mock 계층은 없다**(2026-09-16 폐기, 아래 결정 로그) — 화면은 실 `core-api`에 직접 붙으므로 **백엔드가 떠 있어야 `apps/web`이 선다.**
- **아키텍처**: 단일 FastAPI 백엔드(`apps/core-api`) + PostgreSQL + APScheduler.
- **이번 범위 밖(추후 필요 — 2026-09-03 회의)**: 근거 조회 · 시계열 차트 · 시간 슬라이드 차트. 현 마일스톤 카드에 넣지 않는다.

### Action Whitelist — 런북 10종 = 본편 7 + 롤백 3 (전부 MVP)

**이 표가 Action Whitelist의 확정본이다.** 여기 없는 Runbook ID는 실행 경로에 진입할 수 없다(가드레일 ② Action Whitelist). 코드 소재는 아래 §코드 소재이며, 코드와 이 표가 어긋나면 **이 표를 기준으로 코드를 고친다**.

| 분류 | Runbook ID | 위험도 / 승인 | AI 추천 | 등록 롤백 |
| --- | --- | --- | --- | --- |
| SecOps | `RUNBOOK_EC2_ISOLATE` | High / 0.5초 선차단·1분 타임아웃 | ✅ | `RUNBOOK_EC2_UNISOLATE` |
| SecOps | `RUNBOOK_NACL_ADD_DENY` | Medium / 관제자 승인 | ✅ | — (해제는 `NACL_RESTORE` 주 조치 경로) |
| SecOps | `RUNBOOK_NACL_RESTORE` | Low / 관제자 승인 (원클릭 해제) | ✅ | — |
| SecOps | `RUNBOOK_SG_DELETE_ISOLATED` | Medium / 관제자 승인 | ✅ | `RUNBOOK_SG_RECREATE` |
| FinOps | `RUNBOOK_EC2_RIGHTSIZING` | Medium / 관제자 승인 (자동 원복 시연 대상) | ✅ | `RUNBOOK_EC2_REVERT_SIZE` |
| FinOps | `RUNBOOK_EC2_ENABLE_AUTOSCALING` | Medium / 관제자 승인 (stateless 한정 구조 전환) | ✅ | — |
| FinOps | `RUNBOOK_EBS_DELETE_UNATTACHED` | Low / 관제자 승인 | ✅ | — |
| 롤백 | `RUNBOOK_EC2_UNISOLATE` | Medium / 관제자 승인 (원클릭 해제) | ❌ | — |
| 롤백 | `RUNBOOK_SG_RECREATE` | Low / 관제자 승인 | ❌ | — |
| 롤백 | `RUNBOOK_EC2_REVERT_SIZE` | High / 시스템 자동 발동 (Status Check 실패 시) 또는 관제자 수동 요청 | ❌ | — |

> **분류 축 주의**: 위 `분류`는 Runbook Registry 축(`FINOPS`·`SECOPS`·`ROLLBACK`)이며, Incident 분류(`FINOPS`·`SECOPS`)와 별개다. 롤백 3종은 Registry에서 `ROLLBACK` 단일 도메인으로 묶인다.

**롤백 런북 공통 정책 (2026-08-13 확정)**: ① Whitelist 정식 등록 — 가드레일 우회 경로 없음. ② `ai_recommendable: false` — AI 추천 목록에서 제외, 트리거는 시스템/관제자만. ③ 원복 파라미터는 DB 백업 레코드(`backup_record_id`)에서만 로드. ④ 롤백이 가드레일에서 거절되면 자동 재시도 없이 CRITICAL 알림 + 수동 개입.

#### 코드 소재

| 층 | 파일 | 담는 것 |
| --- | --- | --- |
| ID 판정 | `packages/schemas/runbooks.py` | `RunbookId` 10종 · `is_allowed_runbook()` · `is_ai_recommendable()` · `RunbookDomain` · `RUNBOOK_DOMAIN_BY_ID` · `ROLLBACK_RUNBOOK_BY_MAIN_ID` |
| 실행 축 어휘 | `packages/schemas/runbooks.py` | `TriggerSource` 4종 · `ApprovalMode` 2종 (값 정의는 §확정 결정 로그 2026-08-18) |
| 파라미터 계약 | `packages/schemas/runbook_parameters.py` | 후보 파라미터(본편 7종, 후보 시점에 정해지는 값 — AI 몫과 **서버 계산 몫** `SERVER_COMPUTED_CANDIDATE_PARAMS`) · precheck 파라미터(10종) · `build_precheck_parameters()` |
| 다운사이징 목표 타입 규칙 | `packages/schemas/rightsizing_policy.py` | 패밀리별 사양 표(메모리) · `rightsizing_target_type()` — 비율·하한 값 소유 김승철(#251) |
| 허용 AWS 작업·Dry-Run 지원 | [ADR-0007](adr/0007-guardrail-dryrun-executor-precheck-contract.md) §Context·§5 | 런북별 `target_api` 전수 실측표 · Dry-Run 미지원 5종의 조회 대체 검증 |

## 확정 결정 로그

| 날짜 | 결정 | 기록 |
| --- | --- | --- |
| 2026-08-11 | 단일 FastAPI 백엔드(`core-api`)로 모노레포 통합 | [ADR-0001](adr/0001-mvp-monorepo-structure.md) |
| 2026-08-12 | **본편 7종 전부 MVP 확정** — Action Whitelist 확정본은 위 §Action Whitelist 표 | [ADR-0002](adr/0002-runbook-whitelist-mvp-scope.md) |
| 2026-08-12 | 다운사이징 백업 = **스펙 JSON**(`SAVE_INSTANCE_SPEC_JSON`) 주 방식, EBS 스냅샷은 선택 보조 | [ADR-0002](adr/0002-runbook-whitelist-mvp-scope.md) §Context |
| 2026-08-12 | 관제자 미응답 타임아웃 **1분**(`TIMEOUT_ISOLATION_1M`)으로 통일 (3분 폐기) | [ADR-0002](adr/0002-runbook-whitelist-mvp-scope.md) §Context |
| 2026-08-12 | 구 Whitelist 예시(`RUNBOOK_EC2_DOWNSIZE`, `RUNBOOK_IP_BLOCK`) 폐기 | [ADR-0002](adr/0002-runbook-whitelist-mvp-scope.md) §Decision |
| 2026-08-13 | **FE 스택 Next.js 14 → 16 상향** — 14 라인은 14.2.35에서 동결, 로컬 Node 26 지원 범위 밖, shadcn CLI 4.x가 Tailwind v4/Next 15+ 기준. `apps/web` = Next 16.3.0 + React 19 + Tailwind v4 + shadcn(radix-nova) | [ADR-0003](adr/0003-fe-stack-nextjs-16.md) |
| 2026-08-13 | **개발 환경 = LocalStack, 발표 직전 실 AWS 전환** — `AWS_ENDPOINT_URL` 유무로 전환. 팀 표준 환경(compose·시드·env) 구성은 전략 수립 후 진행 | [ADR-0006](adr/0006-localstack-team-standard-env.md) |
| 2026-08-13 | **롤백 런북 3종 Whitelist 정식 등록(7→10종)** — 우회 정책 기각, `ai_recommendable: false`·백업 레코드 기반 복원·가드레일 실패 시 수동 개입 정책 채택 (미해결 #1 해소) | [ADR-0004](adr/0004-rollback-runbook-whitelist-registration.md) |
| 2026-08-13 | **팀명 = "딸깍 인프라" 확정** — README의 "서버룸 난방공사" 표기는 구버전(갱신 필요) | — |
| 2026-08-13 | **런북 10종 전부 실구현 방침** — mock/영상 대체 컷라인 기각(팀장 결정). P0/P1/P2는 착수 순서로만 운용, 9/13은 중간 점검 시점(→ 2026-09-08 결정으로 **9/11**로 이동) | 본 문서 §일정 리스크 |
| 2026-08-13 | **LangGraph MVP 도입 확정** — 프로젝트 정체성 사유(팀장 결정, "미확정" 상태 종료). AI 파이프라인을 LangGraph 그래프로 구현하되 GPT-4o + Pydantic Structured Output 출력 계약은 불변. 그래프 설계는 안성일 주관(ADR 후보) | 본 문서 §MVP 확정 범위 |
| 2026-08-14 | **API 계약 확정** — Incident·Execute·WebSocket·오류 봉투 DTO 코드화(`packages/schemas/api/`), 실행 상태 4→6종(`ROLLED_BACK`·`ROLLBACK_FAILED` = 복구 최종 결과 추가), health_score 0–100 **정수** 확정 | 이슈 #32 |
| 2026-08-18 | **실행 축 어휘 교체(ADR-0004 1차 개정)** — `approval_mode`·`trigger_source` 두 축의 의미를 의도적으로 교체. `trigger_source`(실행별 기록) = `USER_APPROVAL`·`PRE_MITIGATION_0_5S`·`TIMEOUT_ISOLATION_1M`·`AUTO_ON_FAILURE`, `approval_mode`(런북별 정책) = `HUMAN_ONLY`·`SYSTEM_OR_HUMAN`. 런타임 의미 무변경이라 supersede 없이 1차 개정으로 종결 | [ADR-0004](adr/0004-rollback-runbook-whitelist-registration.md) |
| 2026-08-18 | **LangGraph 그래프 구조 확정** — FinOps·SecOps 두 그래프로 분리, Checkpointer 미사용(업무 상태는 PostgreSQL 단일 원천·그래프 내 승인 중단점 없음), Guardrail·DB 저장·AWS 실행·승인은 그래프 밖, 모델 호출은 `AIModelClient` 경계 경유. 판단 근거는 구조화 필드로만 보존(Prompt 전문·내부 추론 텍스트 미보존) | [ADR-0005](adr/0005-langgraph-stateless-domain-graphs.md) |
| 2026-08-19 | **LocalStack 팀 표준 환경 전략 확정** — 단일 compose(Community 전용·버전 고정), 시드 = Boto3 스크립트 단일 원천(멱등·rule_engine 임계값 결합·실 AWS 실행 거부), `AWS_ENDPOINT_URL` 스위치 규약 전 모듈 승격(환경 감지 분기 금지), 검증 한계 4경로(Dry-Run·Status Check·CloudWatch·ALB TG/ASG)는 6–7주차 실 AWS 스모크로 이월 | [ADR-0006](adr/0006-localstack-team-standard-env.md) |
| 2026-08-24 | **가드레일 ④ AWS Dry-Run = executor `precheck()` 단일 호출로 확정** — 동기·예외 미전파, 반환은 `PrecheckOutcome`(`passed`·`reason_code`·`verification_summary`). `DryRunOperation` **예외 발생만 PASS**(정상 반환은 플래그 미적용으로 보고 FAIL). `DryRun` 미지원 5종(전면 2 = `NACL_ADD_DENY`·`NACL_RESTORE`, 부분 3 = `EC2_ISOLATE`·`EC2_UNISOLATE`의 elbv2 호출·`ENABLE_AUTOSCALING`의 asg 호출)은 환경 무관 **조회(describe) 대체 검증**. 실측 결과 `elbv2`·`autoscaling`은 LocalStack Community 미포함(Pro 전용)이라 P2 3종은 로컬 검증 경로 자체가 없음 → ADR-0006 §4 검증 한계 표에 확정 편입 | [ADR-0007](adr/0007-guardrail-dryrun-executor-precheck-contract.md) / 이슈 #113 / PR #117 |
| 2026-08-25 | **ADR-0007 1차 개정 — `precheck()` 구현 실측 반영** — 판정 구조·사유 코드·대체 검증 5종은 **불변**. ① `DryRun` 통과는 대상 자원 존재를 증명하지 않음(부재 자원에도 `DryRunOperation` 반환) → §3 요약 문구 정정, DryRun 전면 6종에 존재 확인 describe **미추가**로 확정 ② `EC2_UNISOLATE` 조회를 `describe_target_health` → **`describe_target_groups`**(전자의 응답에 `VpcId`가 없어 통과 조건 ③을 확인할 수 없음) ③ §1 시그니처에 키워드 전용 **`backup_loader`** 명시, "예외를 던지지 않는다"의 예외를 **배선 오류 1건**으로 한정 ④ `EC2_ISOLATE` 등록 판별 = `Target.NotRegistered` 기준, `NACL_ADD_DENY` 중복 검사 = 인바운드(`egress=False`) 기준, `NACL_RESTORE` 백업 조회 = `(rule_number, egress)`로 특정 ⑤ **리전 규약 신설** — AWS 클라이언트와 ARN 파라미터 모두 `target_arn`의 리전 기준(기본 리전 고정 시 2번째 리전 자산 오판정) | [ADR-0007](adr/0007-guardrail-dryrun-executor-precheck-contract.md) / 이슈 #133 / 구현 PR #147 |
| 2026-08-24 | **`_is_prod` 운영 자산 인식 기준 확정** — 규칙 소유자(김승철) 결정. **키**(대소문자 무시) = `environment`·`env`·`stage`·`tier`, **값**(소문자 정확일치·부분일치 금지) = `prod`·`production`·`prd`. 부분 문자열 매칭은 영구 금지(`product-service`류 오탐 방지, #81). 접미 변형(`prod-us-east` 등) 미탐은 의도된 결과이며 Golden 경계 케이스로 고정 | 이슈 #95 / PR #97 |
| 2026-08-25 | **`TIMEOUT_ISOLATION_1M` 자동 격리에서 Low 제외 확정** — `AGENT_WAIT`(Medium·Low) 중 **Medium만** 1분 미응답 시 자동 격리한다. `TIMEOUT_ISOLATION_1M`이 부르는 조치가 `RUNBOOK_EC2_ISOLATE`(ALB 타겟 그룹 이탈 + 격리 SG 교체)라, Low 판정 건을 사람 확인 없이 격리하면 오탐 비용이 이득보다 크다. **기준 등급 = `initial_risk_level`**(+ `response_mode = AGENT_WAIT`). `reviewed_risk_level`(AI 정밀 평가)은 초기 판정을 덮어쓰지 않는 관제자 참고값이라 자동 행동을 가르지 않는다 — `response_mode` 자체가 초기 판정에서만 파생되기 때문이다(`packages/schemas/events.py` `_EXPECTED_MODE_BY_RISK`). 정밀 평가에 자동 행동 변경 권한을 주려면 상태 전이 계약(`MEDIUM→LOW` 대기 취소 · `LOW→MEDIUM` 대기 시작 시점 · `→HIGH` 즉시 격리 · `HIGH→하향` 시 이미 수행된 격리 처리)을 먼저 정의해야 하며, 그 전까지는 이 규칙을 확장하지 않는다(2026-08-25 PR #163 리뷰, 안성일). 본 문서 §MVP 확정 범위의 3분기 서술이 Low를 빼지 않아 화면설계서 v1.5 §4.5와 갈려 있던 것을 §4.5 쪽으로 확정한다. 판정 규칙의 코드 확정은 Risk Evaluator(§미해결 6번) 구현과 함께 남긴다 | PM 결정(김세혁) / PR #162 리뷰 |
| 2026-08-25 | **이슈 자동 CLOSE 금지 — 작업자 수동 CLOSE · 머지 승인 코멘트에 Claude CLOSE 추천**(2026-08-26 개정) — 커밋 푸터·PR 본문에 `Closes`·`Fixes`·`Resolves`를 쓰지 않고 **`Refs #N`으로만 연결**한다. 이 저장소는 **기본 브랜치가 `dev`**라 그 키워드가 머지 즉시 이슈를 닫는데, 리뷰 지적의 잔여 작업이 남아 있어도 "닫힘 = 해소"로 보이게 된다(실제 사례: #124·#134가 커밋 푸터로 자동 닫힘, #134는 잔여 때문에 재오픈). **머지와 이슈 종료를 분리**하고, **CLOSE 판단·수행 주체는 그 PR의 머지 책임자(머지를 수행하는 사람)** 로 둔다. 2026-08-26 회의에서 최초안의 **"작성자 외 1명 교차검증" 요건은 삭제**했다 — PR 리뷰가 이미 이슈 범위·승인 기준을 확인하므로 중복 절차다(안성일, PR #175 리뷰). 대신 머지 책임자가 판단을 깜빡하지 않도록 **머지 승인 코멘트 끝에 Claude가 CLOSE 추천 한 줄(`CLOSE 추천 — #N: 근거` / `CLOSE 보류 — #N: 남은 항목`)을 덧붙인다.** 추천은 권고일 뿐 CLOSE 권한이 아니다 — 자동 CLOSE 폐지의 목적인 **휴먼 온 더 루프**를 유지한다. 머지는 주로 김세혁·안성일이 맡으므로 이 규칙이 걸리는 인원은 사실상 그 둘이다. 규약 자체는 2026-08-25에 결정됐으나 어느 문서에도 등재되지 않아 뒤늦게 기록한다 — 코드화 위치는 `CLAUDE.md` §Git 작업 흐름·§커밋 메시지 규칙·§Pull Request(PR) 규칙과 `.github/PULL_REQUEST_TEMPLATE.md`다 | 이슈 #124·#134 코멘트(2026-08-25, 박지현) / 2026-08-26 팀 회의(김세혁·안성일) / PR #175 |
| 2026-08-26 | **ADR-0007 2차 개정 — ④ 사유 코드 정의 위치 현행화** — 판정 구조·코드 6종·값 문자열·호출 규약은 **불변**. `PrecheckReasonCode`의 정의처가 `packages/schemas/precheck.py` → **`packages/schemas/guardrails.py`**(네 단계 공용 목록 — `SCHEMA_`·`WHITELIST_`·`ARN_`·`PRECHECK_` 접두로 거절 기록에서 단계를 역산)로 옮겨간 것을 §1이 따라가지 못하고 있던 것을 맞춤. `precheck.py`는 재노출이라 `from schemas.precheck import PrecheckReasonCode` 경로는 그대로이며, `GuardrailStepResult.reason_code`가 네 단계 Enum union으로 좁혀져 **단계↔코드 정합을 계약이 강제**한다 | [ADR-0007](adr/0007-guardrail-dryrun-executor-precheck-contract.md) / 이슈 #125 / PR #164 |
| 2026-08-26 | **QA 산출물의 DOMAIN 표기 규칙 확정(A안)** — 테스트·데이터셋·시연 시나리오의 DOMAIN은 **파일이 놓인 디렉터리가 아니라 그 작업이 검증·대상으로 삼는 영역**을 따른다(`tests/test_guardrails.py` → `AI`, `datasets/golden/` → `DATA`, 실행 계열 하네스 → `BE`, 문서 자체가 산출물이면 `DOCS`). **`QA` DOMAIN 신설(B안)은 기각** — DOMAIN 축의 실질적 쓸모가 **리뷰어 지정**인데 `QA`로 묶으면 그 연결이 끊긴다(가드레일 테스트 = 안성일, Golden Dataset = 김승철). 이미 #124가 이 기준으로 등록돼 리뷰어가 `DATA` 소유자로 자동 지정됐고 PR #127이 그대로 머지된 선례가 있다. 과거 산출물(#66·#82 / PR #76·#83)의 `[QA]`·`feat/qa/…` 표기 **소급 수정은 하지 않는다**. 코드화 위치는 `CLAUDE.md` §공통 규격: TYPE / DOMAIN | PM 결정(김세혁) / 이슈 #144 |
| 2026-08-27 | **B-Medium 대기 화면의 초 단위 카운트다운 제거 — 표기를 `제안 생성 시간`·`실행 예정 시간` 2종으로 확정** — `AGENT_WAIT`(Medium) 승인 대기 화면은 남은 시간을 초로 세지 않고 **절대 시각 2개**만 보여준다. ① **제안 생성 시간** = 조치 후보가 관제자에게 올라온 시각 ② **실행 예정 시간** = 미응답 시 `TIMEOUT_ISOLATION_1M` 자동 격리가 발동하는 시각. 현행 구현(`apps/web/src/components/incidents/timeout-countdown.tsx`, `incident-detail.tsx`의 WS `INCIDENT_UPDATED.occurred_at` 래치 + 60초)은 제거 대상이다. **기준 시각의 원천은 서버로 둔다** — FE가 이벤트 수신값에서 파생하면 소켓이 끊긴 채 들어온 건에서 표기가 통째로 사라지고(현행 fallback 고정 안내문), 클라이언트 시계 오차가 그대로 화면 값이 된다. 다만 **응답 필드 신설은 발동 엔진과 같은 묶음으로 미룬다** — `TIMEOUT_ISOLATION_1M`을 실제로 발동하는 코드가 아직 없고(APScheduler 잡은 수집 파이프라인 1개뿐), `initial_risk_level`을 내는 Risk Evaluator도 미구현(§미해결 6번)이라, 지금 계약에 시각을 실으면 **서버가 지키지 않는 시각을 응답이 약속하게 된다.** 그 묶음에서 함께 확정할 것: ⓐ 필드의 null 규칙(`category`×`status`×`response_mode` 조합 — `IncidentResponse`는 `extra="forbid"`) ⓑ `IncidentListItem`(상세의 부분집합 10필드) 포함 여부 — 빼면 INC-001 승인 대기 카드에서 못 그린다 ⓒ 상태 전이 시 갱신·취소 규칙(2026-08-25 결정이 미정으로 남긴 영역). 그때까지 화면은 현행 파생값으로 **잠정 표기**하되 카운트다운 UI는 먼저 걷어낸다 | PM 결정(김세혁) / 화면 #155(PR #162)·#168(PR #181) 후속 |
| 2026-08-27 | **화면 구조 개편 — 화면설계서 v1.6**(팀 회의 결정) — 6갈래다. ① **자산 목록에서 판정 비대상 4종 제외** — 거르는 기준 필드는 `evaluation_status`이지 `resource_role`이 아니다(후자로 거르면 미연결 EBS가 목록에서 사라져 결정과 정반대가 된다). **토폴로지 뷰에는 남긴다** ② **인시던트 목록 2분할**(`INC-001` 보안 / `INC-004` 자산 신설) + GNB 4탭 — `category` 서버 필터가 이미 있어 **계약 변경은 없다** ③ 이슈 카드 규칙 3개 — 카드 1장 원칙 · 제목 = 위협 이름 · 종료돼도 이름 불변 ④ **종료 판단 도입** — `[격리 유지]` → `[종료 판단]`, ACT-001 C 모달 신설 ⑤ **남은 제안이 있으면 종료 불가** — 계약 변경 없이 화면이 `[종료 처리]`를 잠근다 ⑥ 목록 카드 위험도를 **단일 값**으로(`reviewed ?? initial`) — 정렬 축은 `initial` 유지 | 2026-08-27 팀 회의 / 이슈 #196 · #201 / 반영 PR #197 |
| 2026-09-01 | **AI 기본 모델 확정 — `gpt-5.6-luna` + `reasoning_effort=low`** — 합격선을 "실행으로 나가는 값(`runbook_id`·`target_arn`·`evidence_ids`·후보 개수)의 흔들림 0"으로 잡고 모델·파라미터 14종 조합을 3라운드(모델 호출 1,824회) 실측한 결과. 합격 4열 중 최저가(6케이스×10회 기준 $0.065 — `gpt-5.6-terra`·`gpt-4o`의 9분의 1)이고 세대는 `terra`와 같은 `gpt-5.6`이라 최신성에서 밀리지 않는다. 확정 600회에서 환각 0. 남은 필드 일치율 차이(83% 대 100%)는 전부 `target_instance_type`이며 모델이 아니라 **파라미터 계약에 허용값이 없어서**라 #251로 분리했다 | 이슈 #237 / 비교표 PR #248 / 반영 PR #252 |
| 2026-09-03 | **Incident 상태 축의 이름 = 「레콘(Reckon)」 확정** — 가드레일(*실행해도 되는가*)에 이어지는 두 물음, *실제로 무엇이 일어났는가* → *그 결과를 어떻게 수습·종료하는가*를 다루는 **Incident 상태와 그 상태를 관리하는 일련의 과정**을 부르는 고유명사. '가드레일'처럼 부를 이름이 필요하다는 안성일 제기에서 출발했고, 어원은 reckon(세다·판단하다). 회의록 후보 Issue Assurance · 상태규범집 · 상태법전과 Slack 제안(Status Ledger · State Chronicle · Custodia · Dossier · Vitals · Acta · 테세라 등) 중에서 골랐다 | 2026-09-03 팀 회의(기록 박지현) / Slack `#회의` |
| 2026-09-03 | **SSOT 갱신 주 2회(월·목)로 변경** — 기존 매주 월요일 1회 → **월요일 아침**(주차 전환·완료 요약·담당별 다음 단계·주차 종료 판정 기준) + **목요일 회의 뒤**(그 회의의 확정 결정·액션 아이템·담당별 현황 반영. 주차 전환·판정 기준은 건드리지 않는다). 갱신 주체는 PM(김세혁) 단독 그대로이고, "일반 PR은 SSOT를 건드리지 않는다"와 그 밖의 날의 주중 예외 규칙도 그대로다. 코드화 위치는 `CLAUDE.md` §갱신 주체와 시점과 `.github/PULL_REQUEST_TEMPLATE.md` 주석 | 2026-09-03 팀 회의 / 반영 = 본 갱신 PR |
| 2026-09-03 | **PR 리뷰 코멘트에 리뷰 상태 변경 필수** — PR에 리뷰 코멘트를 달 때 GitHub 리뷰 상태를 반드시 함께 바꾼다: 수정이 필요하면 `Request changes`, 머지해도 되면 `Approve`. 상태 없는 `Comment`만 남기지 않는다 — 그 코멘트가 머지를 막는지 저자와 머지 책임자가 알 수 없다. 코드화 위치는 `CLAUDE.md` §Pull Request(PR) 규칙 §리뷰/머지 | 2026-09-03 팀 회의 |
| 2026-09-03 | **유사 서비스 제작을 5주차(9/07–9/11)로 연기 · 근거 조회·시계열 차트·시간 슬라이드 차트는 이번 범위 밖** — 유사 서비스 제작(9/02 지시 "FE 유사서비스 비교"의 후속 — 담당은 회의록에 미기재)은 4주차 범위에서 제외한다. 백엔드 연동 완성은 6주차(9/14–9/18)로 잡는다. 세 항목은 "추후 필요"로 분류해 현 마일스톤 카드에 넣지 않는다 | 2026-09-03 팀 회의 |
| 2026-09-07 | **조치별 절감 예상을 MVP 범위에 넣는다 — 금액과 산출 근거를 모두 AI가 낸다** — 자산 인스턴스 수정 추천을 실행할 때 **얼마를 아낄 수 있는지**를 관제자가 확인할 수 있게 한다. ① **값과 산출 근거를 AI가 함께 낸다** — 추천 호출에 실어 받는다. 단가표를 사람이 들고 유지하거나 Cost Explorer·Price List API를 붙이지 않는다(#267 4번이 "산출 근거의 소유자 미정"을 되살리기 전 선행 조건으로 걸었던 항목의 답이다) ② **근거는 구조화 필드로 저장하고 저장분을 재사용한다** — 볼 때마다 다시 부르지 않는다. 판단 근거를 구조화 필드로만 보존하는 [ADR-0005](adr/0005-langgraph-stateless-domain-graphs.md) 원칙을 따른다 ③ **범위는 개별 추천의 절감 예상 하나** — 비용 추이 그래프·누적 절감 캘큘레이터(집계·추이 축)는 범위 밖이다 ④ **설계 조건 2개**: 추정값임이 필드 이름·화면 표기로 **서버 파생 사실값과 구분**되어야 한다(현행 승인 근거 계약이 사실값만 싣는다 — #154 → #178 → #183), 금액은 실행으로 나가는 값이 아니라 #237 합격선 밖이므로 **재현성·환각을 재는 자리를 따로 둔다**. 계약 소유는 안성일(AI 출력 계약), 화면 표시는 유건희 | PM 결정(김세혁) / 이슈 #267 4번 |
| 2026-09-08 | **발표 3건의 날짜 확정 + 마일스톤에서 주말·공휴일 제외** — 5갈래다. ① **기획 발표 9/04(금) 완료** — 기획·문서 단계까지이며 구현 시연은 없었다 ② **중간 발표 10/01(목) = MVP 시연** — 종전에 "1차 발표 10/15"로 적혀 있던 자리이며 **2주 당겨졌다**. 8–9주차(9/28–10/15) 시연 준비 구간이 **8주차 4일**로 줄고, 직전 7주차는 추석으로 **3일**이다 ③ **최종 발표 12/11(금) = Post-MVP 범위 배포 및 시연** — 신설. 범위는 중간 발표 뒤에 정한다 ④ **마일스톤 지정에서 주말·공휴일을 뺀다** — 주차 구간은 평일만 세고 종료일은 금요일(연휴가 걸리면 그 직전 평일)이다 ⑤ **9/13 게이트 → 9/11(금)로 이동, 리허설 1차는 폐지** — 9/13이 일요일이라 ④에 걸린다. 종전 "9/11 리허설 → 9/13 게이트" 2단을 **9/11 하루의 게이트 판정 1단으로 합치고**, `docs/E2E_REHEARSAL_1ST.md`를 [`docs/E2E_GATE_0911.md`](E2E_GATE_0911.md)로 옮겨 그 문서가 게이트 대본이자 판정서가 된다. 이 게이트는 **팀 내부 판정이며 외부 심사자가 오지 않는다** | PM 결정(김세혁) / 반영 = 본 갱신 PR |
| 2026-09-07 | **유사 서비스 조사·화면 제작은 5주차 내 완료 · 담당 = 유건희 + 김승철 협업** — 2026-09-03 회의가 5주차로 연기하면서 담당을 회의록에 남기지 않은 것을 확정한다. 이로써 미결로 남아 있던 **A2(김승철 FE 작업 합류)도 확정**된다 | PM 결정(김세혁) / 2026-09-03 팀 회의 A2·D1 |
| 2026-09-14 | **MVP 일정 재편 — 마감 판정일 10/15(목) 신설, 실 AWS 스모크를 9주차로 이동** — 4갈래다. ① **중간 발표 10/1(목)은 고정점 그대로다** — 2026-09-08 결정(2주 당김)은 유지된다 ② **MVP 마감 판정일 10/15(목)을 신설한다** — 발표 자리가 아니라 내부 완료 판정일이며, 종전에는 10/1(목) 발표로 MVP가 끝나는 구조였다 ③ **실 AWS 스모크를 7주차(9/21–9/23)에서 9주차(10/02–10/08)로 옮긴다** — [ADR-0006](adr/0006-localstack-team-standard-env.md) §4 이월 목록 해소가 이때다. 7주차는 비지 않고 P1 2종·LocalStack 전 시나리오 실경로화·기획서 초안으로 채운다 ④ **P2 3종(`EC2_ISOLATE`·`UNISOLATE`·`ENABLE_AUTOSCALING`)의 실 AWS 첫 검증은 10주차(10/12–10/15)다** — `elbv2`·`autoscaling`이 LocalStack Community에 없어 로컬 검증 경로 자체가 없고, 스모크 환경이 P2 착수의 전제 조건이다. **Post-MVP 시작은 10/02(금)에서 10/16(금)으로 밀린다** | PM 결정(김세혁) / 반영 = 본 갱신 PR |
| 2026-09-14 | **10/1(목) 중간 발표 시연은 LocalStack 기반으로 한다 — 의도된 결정이며 미비가 아니다** — 심사자 앞에서 도는 것이 실 AWS가 아니라 팀 표준 가짜 AWS라는 뜻이고, 이를 감수한다. 실 AWS 전환(더미 자격증명 교체 + `AWS_ENDPOINT_URL` 제거)은 **9주차(10/02–10/08) 스모크에서 처음 수행한다** — 종전 [ADR-0006](adr/0006-localstack-team-standard-env.md) §5가 "8주차(중간 발표 직전) 전환 리허설"로 적어 둔 자리이며 그 행은 이 결정에 맞춰 개정한다. 함께 **시연 축과 개발 축을 브랜치로 가른다** — 시연은 `main`(9/28(월) 릴리스 컷 + 태그로 기준 커밋 고정), 개발은 `dev`가 계속 흐른다. **8주차(9/28–10/01)에 `dev`를 얼리지 않으므로 담당별 작업 배정이 발표를 이유로 끊기지 않는다.** 리허설 중 치명 결함은 `fix/` 브랜치 → `dev` PR → `main` 재컷으로 반영하며 `main` 직접 푸시는 하지 않는다 | PM 결정(김세혁) |
| 2026-09-14 | **10/1(목) 중간 발표 제출물로 프로젝트 기획서를 낸다 — 담당 박지현** — 9/4(금) 기획 발표 제출본은 일정표가 "10/15 MVP 시연" 기준이라 이번 재편으로 뒤집혔고, 그 사이 ADR 5건(0004–0008)과 조치별 절감 예상 MVP 편입이 얹혀 **사실상 재작성이다**. ① **본문은 `docs/PROJECT_PROPOSAL.md`로 저장소에 둔다** — 제출용 변환본은 그것에서 낸다. 종전 제출본은 저장소 밖(로컬 볼트)에 있어 팀이 열어 대조할 수 없었고 완료 판정도 검증할 수 없었다 ② **필수 2항목은 개발 일정(WBS)과 팀원 R&R이며, 원천은 이 문서의 §현재 위치 주차 표와 §팀 & 역할이다** — 기억으로 적지 않고 그 표에서 옮긴다 ③ **착수는 7주차(9/21–9/23), PM 검토 9/28(월), 제출 10/1(목)** — 8주차(9/28–10/01)에 두면 같은 담당자의 리허설과 같은 날 끝나야 한다 | PM 결정(김세혁) / 이슈 #335 |
| 2026-09-08 | **NACL 규칙의 `Protocol`은 AWS 번호 표기로 보내고 저장한다** — LocalStack은 보낸 문자열을 그대로 저장하고 실 AWS는 번호로 정규화해(2026-09-08 실측), 이름으로 보내면 `NACL_RESTORE`의 백업 fingerprint 대조가 LocalStack에서만 맞는다. 실 AWS 스모크로 이월하지 않고 **삽입 시점 번호 변환으로 닫는다**(`schemas.runbook_parameters.NACL_PROTOCOL_NUMBERS`) → ADR-0006 §4 검증 한계 표 6행 편입 | [ADR-0006](adr/0006-localstack-team-standard-env.md) / 이슈 #297 / PR #313 |
| 2026-09-14 | **실행 상태 6→7종 — `UNVERIFIED`(결과 확인 불가) 신설과 판정 불가 재시도 상한** — AWS 조회 실패로 종료 판정을 내리지 못한 실행은 사유를 typed로 저장하고, `PRECHECK_AWS_ERROR`만 설정값(기본 5회·60초)만큼 다시 묻는다. 재시도를 다 쓰거나 다시 물어도 같은 사유(권한 거부 등)면 **자동 원복하지 않고** 확정한다 — 단계가 있는 원본은 `UNVERIFIED`(종료 · 관제자 복구 가능 · 인시던트 `FAILED`), 롤백 자식과 실행 전 대조는 `FAILED`. 기존 6종은 모두 맞지 않았다: `FAILED`는 관제자 복구 목록을 닫고, `ROLLBACK_INITIATED`는 자동 원복을 발동하며, `IN_PROGRESS` 유지는 인시던트를 '조치 진행 중'에 영원히 묶는다 | 이슈 #249 / PR #341 |
| 2026-09-14 | **다운사이징 목표 타입은 AI가 아니라 서버 규칙이 계산한다 — 같은 패밀리 · 현재 메모리의 1/4 이상 · 메모리 2 GiB 이상 중 가장 작은 크기** — #237 계측에서 모델·파라미터 14종 조합 전부 흔들린 유일한 필드(`target_instance_type`)를 AI 출력 스키마에서 빼고 그래프가 자산 스냅샷으로 계산한다. 발의안의 `small` 단독 하한 대신 김승철 안(메모리 1/4 비율 상한 병행)을 채택했다 — 판정은 CPU만 보고 메모리를 수집하지 않으며, 앱만 OOM으로 죽은 상태는 2/2 Status Check를 통과해 자동 원복이 걸리지 않는다. 과소 축소를 화면에 띄울 #250 1번이 Post-MVP로 내려가 "관제자 상주"가 MVP에서 화면 근거를 잃은 것이 보수 쪽을 고른 결정적 이유다. 결과는 `t3.xlarge→t3.medium` · `t3.large→t3.small` · `m5.2xlarge→m5.large`이고, 규칙이 답을 못 내는 자산에는 다운사이징을 조치 메뉴에 올리지 않는다. 비율은 실 AWS 스모크의 임계치 보정 뒤 1/8로 푸는 것을 다시 본다 | 이슈 #251 / PR #345 |
| 2026-09-14 | **푸시 전 로컬 통합 테스트 기준 — Docker를 켜는 조건과 skip 0건 판정** — PR이 추가·수정한 테스트 파일에서 Docker 없이 **무조건 보류(조건 없는 `@pytest.mark.skip`) 외의 skip**이 하나라도 나면 Docker(`docker compose up --wait db localstack` → 시드 → `pytest -rs`)로 다시 돌린다. 그 파일들의 skip이 무조건 보류 말고 0건일 때만 PR 본문 `pytest` 칸을 체크한다. 그 밖의 변경은 CI에 맡긴다 — CI는 DB 접속을 먼저 확인하고(#92) LocalStack을 시드한 뒤 돌기 때문에 인프라 사유 skip이 생기지 않는다. skip 사유 문구로 가르지 않는 것은 문구가 `미기동`·`미설정`·`시드 인스턴스 없음`·`running EC2 없음`으로 제각각이기 때문이다. 코드화 위치는 `CLAUDE.md` §푸시 전 로컬 통합 테스트와 `.github/PULL_REQUEST_TEMPLATE.md`의 `pytest` 칸 | PM 결정(김세혁) / PR #338 |
| 2026-09-14 | **AI 서명 금지를 CI로 강제한다** — 커밋 메시지와 PR 본문의 `Co-Authored-By: <AI>`·`Claude-Session:` 트레일러, claude.ai 세션 링크, `Generated with …` 문구를 전부 금지하고 **`ai-signature` 잡([`scripts/check_ai_signature.py`](../scripts/check_ai_signature.py))이 PR마다 검사해 걸리면 실패시킨다.** 저장소 `.claude/settings.json`의 `attribution`이 Claude Code의 자동 삽입을 끈다. 이로써 `dev`·`main`의 필수 체크가 `test`·`web` 2종에서 **`test`·`web`·`ai-signature` 3종**이 됐다. 커밋은 팀원 본인 이름으로 나가야 하고, 서명이 섞이면 저작 이력이 사람과 도구 사이에서 흐려진다 | PM 결정(김세혁) / PR #340 |
| 2026-09-16 | **팀 5인 → 4인 재편 — 유건희(Frontend) 이탈, 범위 축소 없이 담당 재배치** — 긴급 회의 결정. ① **`FE` 전부를 김세혁이 인수한다**(대시보드·REST/WebSocket 연동·토폴로지 시각화. 기존 `BE`·`INFRA`는 그대로 들고 간다) ② **QA 전부를 김승철이 인수한다**(Golden Dataset·E2E 시연 시나리오·pytest·게이트 판정서. 기존 `DATA`는 그대로) ③ **박지현은 프로젝트 기획서 전담**으로 좁힌다 — Connect Day 제출물(#335)이 평가 대상이라 여기에 집중한다 ④ 안성일의 담당은 바뀌지 않는다. **범위를 줄이지 않은 것은 §일정 리스크의 "미달 시 범위 축소가 아니라 인력 재배치" 방침을 그대로 집행한 것이다.** 함께 정한 운영 규칙 둘 — **담당 경계가 애매한 작업은 팀장이 아침에 판단해 분배한다**(쌓아 두지 않는다), **SSOT 갱신을 기다리며 작업을 멈추지 않는다**(멈춰야 하면 그것은 블로커이고 주중 예외로 처리한다) | 2026-09-16 긴급 회의 / PM 결정(김세혁) |
| 2026-09-16 | **일정 3건 확정 — 9/30(수) E2E 시연물 완성 마감 · Connect Day 10/1(목)–10/2(금) · 10/6(화) 업무 재배정** — 긴급 회의 결정. ① **9/30(수)까지 E2E 시연물을 완성한다** — 10/1(목)에 시연할 경로가 이날 전부 서 있어야 하며, 발표 자리가 아니라 내부 마감이다. **MVP 마감 판정 10/15(목)과 다른 날짜이고 그 판정일은 유지된다** — 9/30은 *10/1에 보일 것*, 10/15는 *실 AWS 스모크와 P2 3종까지 포함한 완료 판정*이다 ② **Connect Day는 10/1(목)–10/2(금) 이틀**이다 — ACC 카페 공간에 섹션을 나눠 멘토를 배정하고 팀당 3–4시간 멘토링을 받으며 **설명–피드백–평가**까지 간다(하루 2세션). **중간 발표는 별도 자리가 아니라 1일차 세션 그 자체다.** 평가가 걸려 있으므로 **프로젝트 기획서를 이 자리에 맞춰 완성한다** ③ **10/6(화)에 업무를 재배정한다** — 회의 발언의 "10월 5일"은 개천절(10/3 토) 대체 휴일이라 "마일스톤은 평일만" 규칙(2026-09-08)에 맞춰 화요일로 옮겼다. **2일차 10/2(금)이 9주차 첫날과 겹쳐 실 AWS 스모크 실작업은 10/6(화)–10/8(목) 3일이 된다** | 2026-09-16 긴급 회의 / PM 결정(김세혁) |
| 2026-09-16 | **FE mock 계층을 폐기하고 실 백엔드에 직접 연동한다** — 긴급 회의 결정이며 구현이 먼저 섰다(#294 작업 브랜치 `ff7f498` → PR #351). `apps/web/src/app/api/v1/**`의 mock Route Handler **6파일(1,762줄)** 을 통째로 지우고 `lib/api/client.ts`의 자체 origin fallback을 없앴다 — 미설정이면 `http://localhost:8000`(compose `core-api`)을 본다. **화면을 mock으로 먼저 세우고 나중에 갈아끼우는 2단 작업이 4인 체제에서 두 배 비용이라** 한 단으로 줄인 것이다. **대가 둘을 받는다** — ① `apps/web`은 이제 **core-api가 떠 있어야** 화면이 선다(`npm run dev`만으로는 전 화면 오류) ② **T1 2단계의 "실패 시 대체 컷 = mock 데이터로 카드 표시"가 사라졌다** — 시연에서 그 컷을 못 쓴다. 낡은 전제가 남은 저장소 문서 4곳은 #352가 닫는다 | 2026-09-16 긴급 회의 / 이슈 #294 · #352 / PR #351 |
| 2026-09-16 | **기획서(#335) 일정을 6주차로 앞당기고 리허설 진행 담당을 정한다 — 박지현 부재 9/20(일)–9/30(수) 반영** — 2026-09-14 결정 ③(착수 7주차(9/21–9/23) · PM 검토 9/28(월))을 **개정한다.** 4갈래다. ① **착수 6주차(9/14–9/18) · 본문 완성 9/18(금) · PM 검토 9/18(금) · 제출 10/1(목)** — 종전 일정은 착수일과 검토일이 **둘 다 부재 구간 안**이라 실행할 수 없고, 9/28(월)에 지적이 나와도 반영할 사람이 없다. 제출물이 비면 **Connect Day 1일차 평가 항목 하나가 통째로 빈다** ② **필수 2항목(WBS·R&R)의 원천은 이 문서(SSOT)이고 값이 다르면 SSOT를 따른다** — Confluence·Slack `문서버전 관리` 재료는 그 밖의 서술·도표·양식에 쓰며 **완료 판정을 대조하는 원천이 아니다**(대조 원천은 저장소 안의 `docs/PROJECT_PROPOSAL.md`와 이 문서다) ③ **9/29(화)–9/30(수) 리허설은 대본 진행·판정 기록 김승철, 운영 머신·환경 김세혁** — 종전에 진행 담당이 문서에 없었고 그 자리로 읽히던 박지현이 그 날짜에 없다 ④ **판정 기준 ⓓ의 이관 원천은 둘이다** — 박지현(대본·사전 준비·T2 축)과 김세혁(T1-3–T1-6 관통 축). `docs/E2E_GATE_0911.md` §6-1이 작성 머신의 R4·R5와 R6 실행 단계를 `🔬미실측`으로 갈랐고 그 축의 실측은 PM 로컬에 있다(#316의 9/10(목) 값이며 9/11(금) 결과로 그대로 전사하지 않는다). **박지현 쪽 인계는 9/18(금)까지 끝낸다** | PM 결정(김세혁) / PR #354 리뷰(박지현·김승철) / 이슈 #335 · #301 |
| 2026-09-16 | **GitHub 글의 정정은 새 글이 아니라 기존 글 수정으로 한다** — 코멘트를 여러 개 달지 않는다. 이미 올린 글이 틀렸으면 그 글을 고치고(`gh api -X PUT …/reviews/{id}` · `gh api -X PATCH …/issues/comments/{id}` · `gh issue edit` · `gh pr edit`), **수정본 맨 위에 무엇을 왜 정정했는지 한 줄**을 남긴다. 정정본을 새 글로 올리면 알림이 두 번 가고 읽는 사람이 **어느 쪽이 최종인지 스스로 가려내야** 한다 — 정정하려던 글이 오히려 최종 요청을 흐린다(실제 사례: PR #326에서 9분 간격 리뷰 2건). **예외는 리뷰 판정이 뒤집힐 때 하나뿐이다** — GitHub이 제출된 리뷰 상태(`CHANGES_REQUESTED` ↔ `APPROVED`)를 본문 수정으로 바꾸지 못하므로, 그때만 새 리뷰를 한 건 올리고 직전 리뷰 본문은 철회 배너로 수정한다. 코드화 위치는 `CLAUDE.md` §GitHub 글의 정정 | 2026-09-16 긴급 회의 / PR #326 |

## API 계약 (확정 — FE↔BE 공개 계약, 코드 원천: `packages/schemas/api/`)

- `GET /api/v1/assets` — EC2/SG 상태·스펙·연결관계·헬스 스코어(**0–100 정수**)·Skip 사유 코드(`SkipReasonCode` **6종** — `SKIP_UNSUPPORTED_STATE`는 EBS가 `available`·`in-use` 외 전이·비정상·미상 상태일 때의 **판정 보류**이며 2026-09-03 #276 / PR #284로 추가됐다)
- `GET /api/v1/incidents` — 목록(상세의 부분집합 10필드 + nullable `title`). `status`·`category` 필터, `created_at` 내림차순 전체 반환(페이지네이션 Post-MVP)
- `GET /api/v1/incidents/{id}` — nullable `title`, AI CoT 3줄 요약, Evidence ID, 추천 Runbook(본편 7종만)·실행 요약(관제자 복구 조치는 롤백 3종만). 추천 항목의 `display_parameters`는 **화면 표시 전용**이며 **서버가 typed `parameters`에서 파생**한다 — LLM이 짓지 않고, 실행 요청(`POST /actions/execute`)에 되돌려 받지도 않는다(2026-08-27, #154 / PR #178) 실행 요약의 `verification_hold`는 **판정 불가 보류 기록**(사유 코드·횟수·처음/마지막 실패 시각)이며 없으면 null이다 — `UNVERIFIED`면 반드시 있고 `SUCCESS`·`ROLLBACK_INITIATED`에는 오지 않는다(2026-09-14, #249 / PR #341)
- `POST /api/v1/actions/execute`
  - Request: `{ incident_id, runbook_id, idempotency_key }` — 추가 필드 거부, Target ARN·AWS 파라미터는 받지 않음
  - HTTP: 신규 접수 **`202 Accepted`**, 같은 `idempotency_key` 재요청 **`200 OK`**(멱등 재생 — 본문은 두 경우가 같다)
  - Response status: `IN_PROGRESS | SUCCESS | FAILED | ROLLBACK_INITIATED | ROLLED_BACK | ROLLBACK_FAILED | UNVERIFIED` — `ROLLED_BACK`·`ROLLBACK_FAILED` = 복구 최종 결과, `UNVERIFIED` = **결과 확인 불가**(AWS 조회 실패로 판정 재시도를 소진해 자동 판정을 멈춘 종료 상태 — 자동 원복하지 않고 관제자 원복을 연다). 셋 다 원본 Execution에만 기록
- WebSocket `/api/v1/ws` — `INCIDENT_CREATED | INCIDENT_UPDATED | EXECUTION_UPDATED` 이벤트 봉투 (DB commit 이후 전송, 상태 원본 아님)
- REST 공통 오류 봉투 `{"error": {code, message, request_id}}` — 코드 5종(404·409×2·422·500)

## 팀 & 역할

**이 표가 역할·담당 경계의 유일한 기준이다.** `CLAUDE.md`·PR 템플릿은 리뷰어 지정에서 이 표를 참조하며 이름을 복제하지 않는다. (README는 포트폴리오 문서라 예외 — 소개용 서술을 별도로 갖는다.)

> 🔴 **2026-09-16(수)부터 4인이다 — 유건희(Frontend) 이탈.** 긴급 회의에서 **범위를 줄이지 않고 담당을 재배치**하기로 했다(§일정 리스크의 "미달 시 인력 재배치" 방침 집행). 이관은 셋이다 — ① **`FE` 전부 → 김세혁** ② **QA(Golden Dataset·E2E 시나리오·pytest·게이트 판정서) 전부 → 김승철** ③ **박지현은 프로젝트 기획서 전담**(Connect Day 10/1(목)–10/2(금) 제출물 · #335). 안성일의 담당은 바뀌지 않았다.
>
> 🔴 **박지현은 9/20(일)–9/30(수) 자리를 비운다.** 7주차(9/21–9/23) 전체와 8주차(9/28–10/01)의 릴리스 컷 9/28(월)·리허설 9/29(화)–9/30(수)가 그 구간이다. 그래서 **기획서 일정을 6주차(9/14–9/18)로 앞당기고**, **리허설 진행 담당을 따로 정했으며**, **판정 기준 ⓓ의 박지현 쪽 인계를 9/18(금)까지로 못 박았다**(아래 §확정 결정 로그 2026-09-16).

| 이름 | 역할 | 담당 | 담당 DOMAIN (리뷰 요청 대상) |
| --- | --- | --- | --- |
| **김세혁** (팀장/PM) | Infra & DevSecOps **+ Frontend** | Boto3 EC2/SG 제어, 스펙 JSON 백업/자동 원복 엔진, Docker·배포, Code Review·브랜치 관리, **Next.js 대시보드 전부**(2026-09-16 인수) | `BE`(Boto3 실행·롤백) · `INFRA`(Docker·CI·IaC) · **`FE`(대시보드)** |
| **안성일** | AI / Guardrail · Architect | 아키텍처·DB 스키마, FastAPI 메인·PostgreSQL, OpenAI 모델 + 4단계 가드레일 | `AI`(가드레일·CoT) · FastAPI 아키텍처 · DB |
| **김승철** | Data & Rule Engine **+ QA/Scenario** | CloudWatch 수집 파이프라인, Rule Evaluator(Idle EC2·미사용 SG, Skip 사유 적재), **Golden Dataset·E2E 시연 시나리오·pytest·게이트 판정서**(2026-09-16 인수) | `DATA`(자산/메트릭 수집 · Rule Engine) · **QA 시나리오** |
| **박지현** | Technical Writer | **프로젝트 기획서 전담**, 저장소 문서화 | `DOCS` |

**복수 담당 접점**

| 변경 접점 | 리뷰 요청 대상 |
| --- | --- |
| `SEC`(SOAR 차단) | 안성일 + 김세혁 |
| `SCHEMA` · API 계약(`assets`·`incidents`·`actions/execute`) 변경 | 계약 **양쪽** 담당자 모두 (BE↔FE, BE↔AI) |

> ⚠️ **FE↔BE 접점은 2026-09-16부터 리뷰어가 한 명으로 줄어든다** — `FE`와 `BE`를 김세혁이 함께 들기 때문이다. GitHub은 저자 본인의 `Approve`를 막으므로 **그 접점의 PR은 안성일이 리뷰한다**(FastAPI 아키텍처·DB·`packages/schemas` 소유). 계약 변경을 저자 혼자 판단하고 머지하는 경로를 만들지 않기 위한 것이며, **"최소 1명 승인" 요건을 FE 변경이라고 면제하지 않는다**(`CLAUDE.md` §리뷰/머지).

> **테스트·데이터셋·QA 산출물의 DOMAIN**(2026-08-26 확정 · 이슈 #144)은 파일이 놓인 디렉터리가 아니라 **그 작업이 검증·대상으로 삼는 영역**을 따른다. `QA`는 DOMAIN 값이 아니다 — DOMAIN 축의 실질적 쓸모가 이 표의 리뷰어 지정이기 때문이다. 표기 규칙 본문은 `CLAUDE.md` §공통 규격: TYPE / DOMAIN.

**담당 경계가 애매한 작업 — 팀장이 아침에 판단해 분배한다**(2026-09-16 확정). 4인 재편으로 한 사람이 여러 DOMAIN을 들게 됐고, 새로 생기는 일이 어느 칸에 속하는지 표만으로 갈리지 않는 경우가 늘었다. **갈리지 않는 일은 쌓아 두지 말고 팀장에게 올린다** — 매일 아침 팀장이 배정한다. 첫 사례였던 [#352](https://github.com/ProjectVigilantis/vigilantis/issues/352)(FE mock 제거 후속 문서)는 **재편으로 소유가 셋에서 둘로 줄어 배정 보류 사유가 사라졌다** — 시연 문서 2곳·골든 README는 김승철, PR 템플릿·FE 실행 절차·대시보드 주석은 김세혁이다.

**SSOT 갱신을 기다리며 작업을 멈추지 않는다**(2026-09-16 확정). 이 문서는 주 2회만 갱신되므로, 갱신일 사이에 정해진 것은 이슈·PR·Slack이 먼저 알린다. **등재를 기다리느라 멈출 일이 있으면 그것은 블로커이고, 블로커는 주중 예외로 처리한다**(`CLAUDE.md` §갱신 주체와 시점).

**안성일은 Claude Code를 쓰지 않는다**(2026-09-16 회의 고지). 브랜치명·커밋/PR 제목 형식·리뷰 상태 규칙 등 `CLAUDE.md` 규약이 자동으로 걸리지 않으므로, **어긋난 것을 보면 리뷰나 코멘트로 알린다.** 본인이 요청한 사항이다.

## 미해결 이슈 / 할 일 (블로커 순)

1. ~~LocalStack 팀 표준 환경~~ ✅ 해소(#62) — [ADR-0006](adr/0006-localstack-team-standard-env.md) 전략 + compose `localstack` 서비스 + `scripts/seed_localstack.py` + `.env.example` 스위치 활성화. 잔여 후속 중 **CI LocalStack service container는 완료**(#65). 남은 것은 **9주차(10/02–10/08) 실 AWS 스모크 테스트**(ADR-0006 §4 — 2026-09-14 일정 재편으로 7주차에서 옮겼다). **범위 확대(2026-08-24, ADR-0007 실측)**: `elbv2`·`autoscaling`이 LocalStack Community에 없어(Pro 전용) `EC2_ISOLATE`·`UNISOLATE`·`ENABLE_AUTOSCALING` **P2 3종은 실행뿐 아니라 Dry-Run 대체 조회조차 로컬에서 돌지 않는다.** 이 3종의 유일한 검증 경로가 실 AWS 스모크이므로, §일정 리스크의 "P2 인프라 조기 준비"가 선택이 아니라 전제 조건이 됐다. **2026-09-14 재편 기준으로 스모크는 9주차(10/02–10/08), P2 3종 실 AWS 첫 검증은 10주차(10/12–10/15)다.**
2. ~~PR #29 후속 보완~~ ✅ 종결(2026-08-19, PM 대행 판단 — 김승철 부재, 기록: 이슈 #67 댓글) — 당초 우려("자체 boto3 로직·시드 없으면 빈 결과 통과")는 현행 `test_collector_raw.py`에서 해소 확인(`collect_region()` 직접 호출, 시드 없으면 assert 실패). dev 머지본(#64) 기준 작성자 외 로컬에서 표준 절차(compose→시드→pytest) 통합 테스트 3건 통과 재현. 잔여였던 **CI LocalStack service container**는 #65로 완료. 김승철 복귀 후 이견 시 재오픈.
3. ~~README 최신화~~ ✅ 해소(팀명·역할 표·런북 10종·LangGraph 반영). README는 포트폴리오·소개용 문서라 갱신 주기가 길다(§문서 지도 5번).
4. ~~**`_is_prod` 판정 규칙**~~ ✅ **완전 종결**(2026-08-25) — 박지현 제기 #81 → 규칙 소유자 김승철 확정(#95 / PR #97). 인식 태그 키·값 집합을 확정·구현했다. **키**(대소문자 무시 정확일치): `environment`·`env`·`stage`·`tier`. **값**(`strip` 후 소문자 정확일치, 부분일치 영구 금지): `prod`·`production`·`prd`. 잔여 2건도 닫혔다 — ① `evaluate_ec2`의 `name` 인자 정리 = #96 / PR #110(인자 자체 제거) ② 확정 기준 반영 Golden 경계 케이스 = #124 / 본 PR(`asset_inventory_003.json` A11–A16, 뮤테이션 4종 방어 확인). 접미 변형(`prod-us-east`)·부정 접두(`non-prod`)·부분 포함(`product-service`) 미탐이 **의도된 결과임이 정답지로 고정**됐으므로, 앞으로 이를 버그로 보고 부분일치를 되살리는 변경은 골든 회귀에서 막힌다.
5. ~~CI에 PostgreSQL service container 없음~~ ✅ 해소(#92, 박지현 제기 2026-08-20) — `ci.yml` `test` 잡에 `postgres:16-alpine` service container를 추가했다(이미지·계정·DB명은 compose `db` 서비스와 동일 — 로컬 = CI 동형). 그동안 CI에서 항상 skip되던 **DB 통합 테스트 28건**(`apps/core-api/db/tests` 21 + `apps/core-api/tests` 7)이 실행되며, `health_score` 0–100 정수 변환 등 DB 경유 계약과 Alembic `upgrade head`가 매 PR에서 검증된다. 서비스 장애로 다시 조용히 skip되는 것을 막기 위해 pytest 앞에 접속 확인 스텝을 둔다. **원 제기 문구 정정**: `test_persistence_pipeline`은 저장소에 존재하지 않는 테스트 이름이며(실제 대상은 위 28건), "`ci.yml` 주석에도 명시"는 #65(PR #84)에서 헤더 주석이 교체되며 사라진 서술이다.
6. ~~**Golden Dataset SecOps 정답 보류**~~ ✅ **해소(2026-08-31)** — 박지현. 블로커였던 판정 규칙이 Risk Evaluator 구현(#210 / PR #206)으로 코드에 확정되면서 추측 없이 정답을 도출할 수 있게 됐고, SecOps 정답 **12건**(PR #223 10건 + PR #242 SSH MEDIUM 밴드 2건)으로 채웠다. FinOps 자산 18건과 합쳐 **골든 30건 정답 100%**이며 4주차 판정 기준 ⓔ가 이것으로 충족됐다(건수는 4주차 종료 시점 실측 — 현재값은 §MVP 확정 범위의 「Golden Dataset 현재값」). 각 케이스가 강제하는 판정 논점은 `datasets/golden/secops/expected/README.md`에 남아 있다. **같은 묶음에 묶여 있던 것(2026-08-27)은 아직 열려 있다** — B-Medium `실행 예정 시간`의 서버 응답 필드 신설. 선행 둘 중 `initial_risk_level` 산출은 위 #210으로 풀렸으나, **`TIMEOUT_ISOLATION_1M`을 실제로 발동하는 코드가 여전히 없어** 서버가 그 시각을 보장할 수 없다. 그 발동 주체가 설 때 함께 연다.

## 일정 리스크 & 구현 우선순위 (2026-08-13 방침 확정)

**런북 10종 전부 실구현이 원칙이다** — mock/영상 대체를 전제한 컷라인("P2 자동 컷")은 채택하지 않는다(팀장 결정). P0/P1/P2는 범위 축소선이 아니라 **구현 착수 순서**로만 사용한다.

- **P0 (최우선 착수, 3주차(8/24–8/28)–5주차(9/07–9/11))** ✅ **4종 전부 dev 진입 완료**: `RIGHTSIZING`+`REVERT_SIZE`(자산 자동 원복 — 4주차 8/31–9/04), `NACL_ADD_DENY`+`NACL_RESTORE`(차단→원클릭 해제 — 5주차 9/07–9/11) — "양방향 회복" 스토리의 골격.
- **P1 (P0 후 순차 — 7주차 9/21(월)–9/23(수))**: `SG_DELETE_ISOLATED`(+`SG_RECREATE`), `EBS_DELETE_UNATTACHED` — 난도 낮음.
- **P2 (조기 준비 병행 — 인프라는 선택이 아니라 전제 조건)**: `EC2_ISOLATE`(+`UNISOLATE`)는 ALB·다중 EC2 시연 인프라가 선행 조건 → 인프라 준비를 앞당긴다. `ENABLE_AUTOSCALING`은 구현량 최대 → 설계 선행. **`elbv2`·`autoscaling`이 LocalStack Community에 없어 이 3종은 Dry-Run 대체 조회조차 로컬에서 돌지 않는다**(ADR-0007 실측, §미해결 1번) → **실 AWS 스모크 환경 확보가 P2 착수의 전제 조건**이며, 지연 시 9주차(10/02–10/08) 스모크가 아니라 P2 구현 자체가 막힌다. **2026-09-14 재편으로 P2 3종의 실 AWS 첫 검증은 10주차(10/12–10/15)에 배치했고**, 그 전제인 계정·ALB·다중 EC2 인프라 준비는 6–7주차(9/14–9/23)에 착수한다.
- **9/11(금) 내부 P0 게이트** ✅ **실시 완료**: P0 4종 실동작 여부 점검(팀 내부 판정 — 외부 심사자 없음). **판정서 기재는 6주차(9/14–9/18)로 넘어갔고 담당이 박지현 → 김승철로 바뀌었다**(§현재 위치 판정 기준 ⓓ · 2026-09-16 4인 재편). 미달 시 범위 축소가 아니라 **인력 재배치·범위 외 작업 중단**으로 대응한다 — **2026-09-16(수) 회의가 실제로 그 방침을 집행했다**(유건희 이탈분을 범위 축소가 아니라 재배치로 흡수). 만회 구간은 6주차(9/14–9/18) 5일 하나이며 — 7주차(9/21–9/23)는 추석으로 3일, 8주차(9/28–10/01)는 **9/30(수) 시연물 완성 마감**까지 3일, 10/1(목) 중간 발표까지 4일뿐이다.

## 문서 지도 (신뢰 우선순위 — 충돌 시 위가 이김)

**프로젝트의 신뢰 우선순위는 이 목록 하나뿐이다.** 다른 문서에 별도 우선순위표를 두지 않는다. 저장소에 없는 파일(`.gitignore` 대상)은 이 목록에 오르지 않으며, 저장소 문서가 그런 파일을 원천으로 지목하지 않는다.

1. **`docs/PROJECT_STATUS.md`** (이 문서) — 범위·결정·역할·현황·Action Whitelist. **대조할 상위 확정본이 없다** — 이 문서만 읽고도 프로젝트를 이해하고 진행할 수 있어야 하며, 부족하면 다른 문서를 가리키는 대신 이 문서를 채운다
2. `docs/adr/` — 결정 배경(왜 그렇게 했나). 결정 자체가 바뀌면 이 문서가 이기고, ADR은 개정 이력으로 따라온다
3. `packages/schemas/` — 계약의 코드 소재(API DTO·Runbook·가드레일). 이 문서의 표와 어긋나면 **이 문서를 기준으로 코드를 고친다**
4. [`docs/E2E_DEMO_SCENARIOS.md`](E2E_DEMO_SCENARIOS.md) — **중간 발표(10/1) 시연 대본의 원천**이자 `tests/test_e2e_scenario.py`의 명세. 위 1–3을 원천으로 삼는 파생 문서라 충돌하면 위가 이긴다
5. [`docs/AI_SUMMARY_BASELINE.md`](AI_SUMMARY_BASELINE.md) — **AI 요약 3줄의 기준선과 재통과 절차**. 요약 프롬프트·모델·`capabilities`를 바꿀 때 무엇을 다시 통과시켜야 하는지의 상시 규약이다. 위 1–3에서 파생한 절차 문서라 충돌하면 위가 이긴다(2026-09-04 PR #288 신설)
6. `README.md` — **포트폴리오·소개용**. 프로젝트를 처음 보는 사람을 위한 문서라 서술이 상세하고 갱신 주기가 길다. 현황·결정의 기준이 아니다
