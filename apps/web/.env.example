# apps/web 환경변수 예시 — 복사해서 .env.local 로 사용 (cp .env.example .env.local)

# 실 BE API 베이스 URL. 미설정이면 자체 origin의 mock Route Handler(src/app/api/v1/**)를 쓴다.
# 설정하면 같은 코드가 실 BE로 나간다 — 전환에 코드 수정은 없다.
#NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
