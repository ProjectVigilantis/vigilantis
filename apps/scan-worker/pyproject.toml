[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scan-worker"
version = "0.1.0"
description = "Vigilantis Scanner Service"
requires-python = ">=3.11"
dependencies = [
    "schemas",
]

[tool.uv.sources]
schemas = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["."]
