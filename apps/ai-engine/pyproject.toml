[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-engine"
version = "0.1.0"
description = "Vigilantis AI Engine Service"
requires-python = ">=3.11"
dependencies = [
    "schemas",
]

[tool.uv.sources]
schemas = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["."]
