# v0.3.0 예비 계측 — Luna/low

**가격 정확도 기준 미충족. 승인 스냅샷 갱신 보류.**

- 실행: 2026-09-15, A1·A11 각 1회. 사용자 승인 범위인 4호출, 재시도 0회, 21.641초.
- 설정: `gpt-5.6-luna`, reasoning `low`, temperature 미지정, 요청 timeout 30초, 최대 시도 1.
- 입력 9,181토큰(캐시 1,682), 출력 1,397토큰. 캐시 할인 없이 usage × 공개 단가로 추정한 비용은 **$0.0035126**이며 청구액 확인은 아니다.
- 모델이 반환한 이름은 `gpt-5.6-luna`였다. 날짜가 붙은 별도 스냅샷 식별자는 받지 못했다.
- 프롬프트 v0.3.0 지문: `9574d258465a02dad5790684e47042625aa652dfe5017598a5ffd9ba7a6f0d3e`.
- 실행 당시 버전 이름은 `v3`였다. 원자료·채점 결과·호출 메타데이터는 당시 값을 보존하며, 새 표기 `v0.3.0`은 같은 지문을 가리킨다.
- 원자료: [raw.json](raw.json), 채점: [scored.json](scored.json), 호출·설정·구현 지문: [metadata.json](metadata.json).

## 관찰

| 케이스 | 변경 사양 | AI 월 절감액 | 참조 월 절감액 | 오차 | 사전 10% 기준 |
| --- | --- | --- | --- | --- | --- |
| A1 | t3.xlarge → t3.medium | $103.37 | $113.88 | −9.23% | 범위 내 |
| A11 | t3.large → t3.small | $45.55 | $56.94 | −20.00% | 초과 |

참조는 `spec.json`에 고정한 서울 리전 Linux/Shared/OnDemand 요금과 월 730시간이다.

| 케이스 | AI 단가(현재 / 목표, USD/h) | 참조 단가 | 단가 오차 |
| --- | --- | --- | --- |
| A1 | 0.188800 / 0.047200 | 0.208000 / 0.052000 | 각각 −9.23% |
| A11 | 0.083200 / 0.020800 | 0.104000 / 0.026000 | 각각 −20.00% |

- 두 건 모두 `(현재 단가 − 목표 단가) × 730` 산술, 대상·사양·리전 식별자, 고정 가정과 근거 필드를 통과했다. 모두 `ESTIMATED`로 수용됐지만 참조 단가는 정확히 맞지 않았다.
- 근거 원문은 두 건 모두 서울 리전 Linux 공유형 온디맨드 요금이라고 서술했다. 계산과 제외 조건, 모델 추정이라는 한계는 기재했지만 리전 이름이 맞는 것으로 해당 리전의 가격 정확성을 보장하지 못했다.
- A1은 허용 오차 안에 들어왔다는 뜻이며 가격이 정확히 일치했다는 뜻은 아니다.
- 그래프 실패·빈 후보·미산출은 0건. 각 1회이므로 반복 안정성은 미측정이다.
- 요약 자동 사실 검사는 2/2 통과했다. A1 요약에는 낮은 CPU 이용률만으로 과대 스펙이 확정됐다고 표현한 부분이 있다. 기존 판과의 짝 비교·요약 판정자는 이번 승인 범위에서 실행하지 않았으므로 회귀 통과나 새 결함으로 확정하지 않는다.

## 판정과 다음 단계

`scored.json`의 `complete=false`는 전체 6건 × 10회가 아닌 예비 실행이라는 뜻이다.
별도로 A11의 가격 오차가 실제 기준을 초과해 `PRICE_DEVIATION`으로 판정됐다. 단순한 반복 수 부족과 가격 실패를 구분한다.

현재 숫자 검증은 잘못된 단가로 정확히 계산한 값을 걸러내지 못한다. 모델 지식에 의존하는 가격 정확도는 별도 해결이 필요하다.
허용 오차를 늘리거나 참조 단가를 모델 입력에 넣어 이번 실패를 통과로 바꾸지 않는다.

다음 후보는 `gpt-5.6-luna/low`를 유지한 프롬프트 v0.4.0이다. 단가의 리전·사양·과금 조건을
먼저 대조한 뒤 월 차액을 계산하도록 추천 프롬프트를 개선한다. 입력·출력 스키마·채점 기준은 고정한다.
A1·A11 각 1회 예비 계측으로 가격 오차와 산출 여부를 함께 확인하며 실행 전 별도 라운드 승인이 필요하다.
후속 실행 결과와 현재 검증 상태는 [평가 안내](../../README.md#프롬프트-v040-계측)를 따른다.
