# SecOps 실험 요약

#380 이후 저장 근거로 측정한 1–9차의 목적·규모·당시 결과를 정리한다. 결함별 수정 근거는 [품질 개선 근거](../defects.md), 현재 규격은 [품질 기준](../baseline.md)에서 확인한다. 모델은 모두 `gpt-5.6-luna / low`이며, 표의 결과는 구조화 값과 의미 검토를 합친 건별 판정이다. 라운드마다 측정 대상·프롬프트·평가 기준이 다를 수 있으므로 점수를 합산하거나 같은 조건의 성능 추이로 읽지 않는다.

## 공개 기준선 — v1.0.0

2026-09-21, v0.5.6의 9차 품질 통과와 최종 기능 회귀를 근거로 같은 내용을 첫 공개 평가 기준선 **v1.0.0**으로 확정했다. 프롬프트·입력·응답 스키마·모델 설정·답지·rubric을 유지한 버전 승격이다. 아래 9차의 측정 버전은 당시의 v0.5.6으로 보존하며 새 실험 회차로 세지 않는다. 현재 승인 지문은 [prompt_snapshot.json](../prompt_snapshot.json)에 있다.

## 1–9차 경과

| 라운드·버전 | 측정 대상·반복 수 | 보완 목적·확인 사항 | 당시 결과 |
| --- | --- | --- | --- |
| 1차 · #380 이후 초기 작업본 | C01–C07 × 1회 = 7건 | 저장 근거·실제 후보를 반영한 첫 평가. C04·C06 위험 등급과 C06 요약 결함 확인 | 5 PASS / 2 FAIL |
| 2차 · v0.5.0 | C04–C06 × 1회 = 3건 | 위험 등급·전체 관측 창·시각 원천 보완. C06의 보류 이유·발췌 해석·기록 부재 표현은 미달 | 2 PASS / 1 FAIL |
| 3차 · v0.5.1 A | C04–C07 × 1회 = 4건 | 보류 이유를 관측에 연결하고 전체 집계·발췌, 기록 부재·실행 부재를 구분 | 4 PASS / 0 FAIL |
| 4차 · v0.5.1 B | C01–C07 × 3회 = 21건 | A와 같은 프롬프트로 전체 반복 측정. C06의 성긴 반복·단발성 표현에 판정 불확실성 확인 | 18 PASS / 0 FAIL / 3 PENDING |
| 5차 · v0.5.2 | C01–C07 × 3회 = 21건 | 단발·반복의 관측 단위와 빈도를 구분. C06 후보 생성·영향 설명이 당시 기준에 미달 | 19 PASS / 2 FAIL |
| 6차 · v0.5.3 | C01–C07 × 3회 = 21건 | 후보·무제안을 모두 허용하도록 프롬프트·답지·rubric 정렬. 연결 서브넷 범위 설명 3건 누락 | 18 PASS / 3 FAIL |
| 7차 · v0.5.4 | C01–C07 × 3회 = 21건 | 출발지 → 연결 서브넷으로 유입 → TCP 전체 포트의 실제 범위를 먼저 설명 | 당시 기준 21 PASS / 0 FAIL |
| 8차 · v0.5.5 | C01–C07 × 3회 = 21건 | 초기 등급 복사 지시 제거·팀 위험 기준 공유·내부 식별자 검사 추가. 필드명 노출과 요약 등급 모순 잔존 | 17 PASS / 4 FAIL |
| 9차 · v0.5.6 | C01–C07 × 3회 = 21건 | 내부 필드명 예문을 관제자 표현으로 바꾸고 직전 AI 등급의 한국어 표기를 요약에 전달 | 21 PASS / 0 FAIL |

1–7차의 실행일은 2026-09-18, 8–9차는 2026-09-19다. 1차에는 v0.5.0 이전의 초기 작업본이라는 범위만 표시하고, 이후 버전을 소급 부여하지 않는다.

## 판정과 기준 변경을 읽는 방법

- **부분 비교와 전체 반복:** 1–3차는 각 입력 1회 관측이며, 4차부터 각 입력 3회로 측정했다. 부분 비교의 통과를 전체 7건 × 3회 기준선에 합산하지 않는다.
- **4차 판정 보류:** C06 세 출력은 생성·검토를 마쳤지만 `risk_reason`이 UNSURE여서 PENDING이다. 확정된 오답이나 생성 누락으로 바꾸어 쓰지 않는다.
- **5차 이후 기준 재검토:** 사용자 판단을 돕는 서비스 역할에 맞춰 후보 제시와 현재 적용 보류를 분리했다. 기존 출력 재검토에서는 제안 기준으로 21 PASS였으나 새 모델 호출은 없었다. 원래 19 PASS / 2 FAIL과 당시 무제안 지시 위반은 보존하며, 바뀐 기준은 v0.5.3의 새 6차 측정부터 적용했다.
- **7차 당시 통과와 추가 검사:** 구조화 21/21·기존 의미 기준 168/168은 당시 통과 기록이다. 이후 리뷰에서 확인한 내부 필드명 노출은 새 결정적 검사에서 21/21 FAIL이었다. 이 재분류를 원래 기록에 덮어쓰거나 새로운 모델 측정으로 세지 않는다.
- **최종 측정 범위:** 9차는 구조화 21/21·의미 기준 168/168·내부 식별자 검사 21/21을 통과했고 후보 20건·무제안 1건을 관측했다. 의미 평가는 assistant의 인용 기반 직접 검토이며, 세 줄을 함께 읽는 기준과 약한 표현의 한계는 아래 최종 측정의 한계에 남겼다.

## 최종 측정 — v0.5.6

| 항목 | 기록 |
| --- | --- |
| 실행 ID·시각 | `20260919T040016Z-7aec24a0`, 2026-09-19 04:00:16 UTC |
| 코드 기준 | `f260bd488f4ab19f315dcec2a8b70658d63a6937`과 #324 작업본. 아래 요청 지문으로 조건 식별 |
| 모델·설정 | `gpt-5.6-luna`, reasoning `low`, temperature 미지정 |
| 실행 | 그래프 21회, 논리 모델 호출 63회, 그래프 소요 합계 112.485초 |
| 오류 | 전송·도구·모델 응답 오류 0, Dispatcher 거절 0 |
| 구조화·의미·식별자 | 21/21, 168/168, 21/21 PASS. 의미 UNSURE 0 |
| 결과 | 21 PASS / 0 FAIL / 0 PENDING, 후보 20건·C07 3회차 무제안 1건 |
| 검토 방식 | assistant의 인용 기반 직접 검토. 별도 유료 판정자 호출 없음 |

제공자가 반환한 모델 식별자는 `gpt-5.6-luna`이며 날짜가 붙은 고정 식별자는 반환되지 않았다. HTTP 시도 횟수는 별도 계측하지 않았으므로 63회는 논리 호출 수다. 입력 286,354토큰(캐시 입력 212,964 포함), 출력 11,346토큰을 사용했고 당시 단가로 계산한 추정은 약 $0.033이다. 청구 확정액은 아니다.

### 최종 보완과 판정 근거

8차의 내부 필드명 노출(C02 2회차·C04 2회차·C06 1회차)과 C04 3회차의 중간→낮음 요약 모순은 9차에서 관측되지 않았다. 21건 모두 실제 재평가와 요약 등급이 일치했고 내부 식별자 검사도 통과했다.

C01–C03은 강한 반복과 높음, C04는 60회/10분·분당 6회와 중간, C05는 5회/10초·분당 30회와 중간을 설명했다. C06은 5회/1시간을 낮은 빈도의 반복으로, C07은 1회를 단발·낮음으로 유지했다. 후보와 무제안의 이유는 각각 관측과 함께 평가했다.

### 표현상의 한계와 미측정 범위

동결 rubric대로 요약 세 줄을 함께 읽었다. 다음 약점을 이유로 사후에 새로운 단일 문장 합격 기준을 추가하지 않았다.

- C04 1·3회차의 “모두 충족하지 않는”은 모호하다. 같은 문장의 횟수·빈도와 임계값을 함께 읽으면 횟수 조건만 충족함을 복원할 수 있어 PASS로 판정했다. 2회차는 이를 직접 명시한다.
- C03 1·2회차와 C04 2회차의 조치 문장은 관측 강도와의 연결을 앞의 진단 설명에 의존한다.
- C07 3회차의 “규칙이 없으므로 현재 적용은 보류”는 이유 연결이 약하다. 단발·낮은 근거 강도와 추가 실패 확인을 세 줄에서 함께 읽어 PASS로 판정했다. 후보 부재만으로 자동 통과시킨 것은 아니다.

이 결과는 합성 SSH 7사례의 관측이다. 무제안 1건으로 다른 입력의 무제안 품질을 일반화하지 않는다. 실제 탐지율·운영 침해·AWS 차단 효과, 선행 실행이 있는 분기, 저장·화면·사용자 종료 통합과 향후 출력의 무실패를 입증하지 않는다.

### 측정 조건 지문

| 항목 | SHA-256 |
| --- | --- |
| 입력 | `03ee93bce312951dd1abb5c8efc3c7471c6bb4d607de8fe9dc274b0f5518307d` |
| 답지 | `b565541f90d630b63177165e3d19998ac64bc7ddbdc38dbf48cae8ea298e77ca` |
| rubric | `62c3c181f23e9cd4b380fd0c174d031bb6f158394705c942a8d3f3827af567e0` |
| 프롬프트 | `07108dfe7743a79e4abf32550efd842417e0e74db3a0cd67bdb3de0661e80ab6` |
| 요청 | `c934bb34c76c022bcd5dc5dfab29d72bec0863f01f118cbe382ecdd55a8f9b8a` |

지문은 측정 조건을 식별하며 의미 판정의 독립 검증을 대신하지 않는다. 기존 결과의 재집계와 새 측정의 선택은 [재검증 절차](../baseline.md#프롬프트입력rubric이-바뀌면-무엇을-다시-돌리는가)를 따른다.
